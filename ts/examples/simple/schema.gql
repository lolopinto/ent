# -----------------------------------------------
# !!! THIS FILE WAS GENERATED BY TYPE-GRAPHQL !!!
# !!!   DO NOT MODIFY THIS FILE BY YOURSELF   !!!
# -----------------------------------------------

type Contact {
  id: ID!
  emailAddress: String!
  firstName: String!
  lastName: String!
}

type Event {
  id: ID!
  name: String!
  startTime: Timestamp!
  endTime: Timestamp
  location: String!
  hosts: [User!]!
  hostCount: Int!
  invited: [User!]!
  invitedCount: Int!
  attending: [User!]!
  attendingCount: Int!
  declined: [User!]!
  declinedCount: Int!
  maybe: [User!]!
  maybeCount: Int!
  creator: User
}

type Mutation {
  userCreate(input: UserCreateInput!): User!
  userEdit(input: UserEditInput!, id: ID!): User
  userDelete(id: ID!): UserDeleteResponse!
}

type Query {
  user(id: ID!): User
  contact(id: ID!): Contact
}

"""
The javascript `Date` as integer. Type represents date and time as number of milliseconds from start of UNIX epoch.
"""
scalar Timestamp

type User {
  id: ID!
  firstName: String!
  lastName: String!
  emailAddress: String!
  accountStatus: String
  createdEvents: [Event!]!
  createdEventsCount: Int!
  friends: [User!]!
  friendCount: Int!
  selfContact: Contact!
  invitedEvents: [Event!]!
  invitedEventsCount: Int!
  eventsAttending: [Event!]!
  eventsAttendingCount: Int!
  declinedEvents: [Event!]!
  declinedEventsCount: Int!
  maybeEvents: [Event!]!
  maybeEventsCount: Int!
  contacts: [Contact!]!
}

input UserCreateInput {
  firstName: String!
  lastName: String!
  emailAddress: String!
}

type UserDeleteResponse {
  deletedUserID: ID!
}

input UserEditInput {
  firstName: String
  lastName: String
}
