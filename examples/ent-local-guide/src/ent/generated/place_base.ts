// Generated by github.com/lolopinto/ent/ent, DO NOT EDIT.

import type {
  Context,
  CustomQuery,
  Data,
  Ent,
  ID,
  LoadEntOptions,
  PrivacyPolicy,
  Viewer,
} from "@snowtop/ent";
import type { GeoPoint } from "@snowtop/ent-postgis";
import type { Field } from "@snowtop/ent/schema";
import type { PlaceDBData } from "./loaders";
import {
  AllowIfViewerPrivacyPolicy,
  loadCustomCount,
  loadCustomData,
  loadCustomEnts,
  loadEnt,
  loadEntViaKey,
  loadEntX,
  loadEntXViaKey,
  loadEnts,
} from "@snowtop/ent";
import { getFields } from "@snowtop/ent/schema";
import { placeLoader, placeLoaderInfo, placeSlugLoader } from "./loaders";
import { NodeType, PlaceCategory } from "./types";
import { PlaceToFansQuery, PlaceToReviewsQuery, User } from "../internal";
import schema from "../../schema/place_schema";

export class PlaceBase implements Ent<Viewer> {
  protected readonly data: PlaceDBData;
  readonly nodeType = NodeType.Place;
  readonly id: ID;
  readonly createdAt: Date;
  readonly updatedAt: Date;
  readonly name: string;
  readonly slug: string;
  readonly description: string | null;
  readonly website: string | null;
  readonly category: PlaceCategory;
  readonly creatorId: ID;
  readonly location: GeoPoint;

  constructor(
    public viewer: Viewer,
    data: Data,
  ) {
    this.id = data.id;
    this.createdAt = data.created_at;
    this.updatedAt = data.updated_at;
    this.name = data.name;
    this.slug = data.slug;
    this.description = data.description;
    this.website = data.website;
    this.category = data.category;
    this.creatorId = data.creator_id;
    this.location = data.location;
    // @ts-expect-error
    this.data = data;
  }

  __setRawDBData<PlaceDBData>(data: PlaceDBData) {}

  /** used by some ent internals to get access to raw db data. should not be depended on. may not always be on the ent **/
  ___getRawDBData(): PlaceDBData {
    return this.data;
  }

  getPrivacyPolicy(): PrivacyPolicy<this, Viewer> {
    return AllowIfViewerPrivacyPolicy;
  }

  static async load<T extends PlaceBase>(
    this: new (
      viewer: Viewer,
      data: Data,
    ) => T,
    viewer: Viewer,
    id: ID,
  ): Promise<T | null> {
    return (await loadEnt(
      viewer,
      id,
      PlaceBase.loaderOptions.apply(this),
    )) as T | null;
  }

  static async loadX<T extends PlaceBase>(
    this: new (
      viewer: Viewer,
      data: Data,
    ) => T,
    viewer: Viewer,
    id: ID,
  ): Promise<T> {
    return (await loadEntX(
      viewer,
      id,
      PlaceBase.loaderOptions.apply(this),
    )) as T;
  }

  static async loadMany<T extends PlaceBase>(
    this: new (
      viewer: Viewer,
      data: Data,
    ) => T,
    viewer: Viewer,
    ...ids: ID[]
  ): Promise<Map<ID, T>> {
    return (await loadEnts(
      viewer,
      PlaceBase.loaderOptions.apply(this),
      ...ids,
    )) as Map<ID, T>;
  }

  static async loadCustom<T extends PlaceBase>(
    this: new (
      viewer: Viewer,
      data: Data,
    ) => T,
    viewer: Viewer,
    query: CustomQuery<PlaceDBData>,
  ): Promise<T[]> {
    return (await loadCustomEnts(
      viewer,
      {
        ...PlaceBase.loaderOptions.apply(this),
        prime: true,
      },
      query,
    )) as T[];
  }

  static async loadCustomData<T extends PlaceBase>(
    this: new (
      viewer: Viewer,
      data: Data,
    ) => T,
    query: CustomQuery<PlaceDBData>,
    context?: Context,
  ): Promise<PlaceDBData[]> {
    return loadCustomData<PlaceDBData, PlaceDBData>(
      {
        ...PlaceBase.loaderOptions.apply(this),
        prime: true,
      },
      query,
      context,
    );
  }

  static async loadCustomCount<T extends PlaceBase>(
    this: new (
      viewer: Viewer,
      data: Data,
    ) => T,
    query: CustomQuery<PlaceDBData>,
    context?: Context,
  ): Promise<number> {
    return loadCustomCount(
      {
        ...PlaceBase.loaderOptions.apply(this),
      },
      query,
      context,
    );
  }

  static async loadRawData<T extends PlaceBase>(
    this: new (
      viewer: Viewer,
      data: Data,
    ) => T,
    id: ID,
    context?: Context,
  ): Promise<PlaceDBData | null> {
    return placeLoader.createLoader(context).load(id);
  }

  static async loadRawDataX<T extends PlaceBase>(
    this: new (
      viewer: Viewer,
      data: Data,
    ) => T,
    id: ID,
    context?: Context,
  ): Promise<PlaceDBData> {
    const row = await placeLoader.createLoader(context).load(id);
    if (!row) {
      throw new Error(`couldn't load row for ${id}`);
    }
    return row;
  }

  static async loadFromSlug<T extends PlaceBase>(
    this: new (
      viewer: Viewer,
      data: Data,
    ) => T,
    viewer: Viewer,
    slug: string,
  ): Promise<T | null> {
    return (await loadEntViaKey(viewer, slug, {
      ...PlaceBase.loaderOptions.apply(this),
      loaderFactory: placeSlugLoader,
    })) as T | null;
  }

  static async loadFromSlugX<T extends PlaceBase>(
    this: new (
      viewer: Viewer,
      data: Data,
    ) => T,
    viewer: Viewer,
    slug: string,
  ): Promise<T> {
    return (await loadEntXViaKey(viewer, slug, {
      ...PlaceBase.loaderOptions.apply(this),
      loaderFactory: placeSlugLoader,
    })) as T;
  }

  static async loadIdFromSlug<T extends PlaceBase>(
    this: new (
      viewer: Viewer,
      data: Data,
    ) => T,
    slug: string,
    context?: Context,
  ): Promise<ID | undefined> {
    const row = await placeSlugLoader.createLoader(context).load(slug);
    return row?.id;
  }

  static async loadRawDataFromSlug<T extends PlaceBase>(
    this: new (
      viewer: Viewer,
      data: Data,
    ) => T,
    slug: string,
    context?: Context,
  ): Promise<PlaceDBData | null> {
    return placeSlugLoader.createLoader(context).load(slug);
  }

  static loaderOptions<T extends PlaceBase>(
    this: new (
      viewer: Viewer,
      data: Data,
    ) => T,
  ): LoadEntOptions<T, Viewer, PlaceDBData> {
    return {
      tableName: placeLoaderInfo.tableName,
      fields: placeLoaderInfo.fields,
      ent: this,
      loaderFactory: placeLoader,
    };
  }

  private static schemaFields: Map<string, Field>;

  private static getSchemaFields(): Map<string, Field> {
    if (PlaceBase.schemaFields != null) {
      return PlaceBase.schemaFields;
    }
    return (PlaceBase.schemaFields = getFields(schema));
  }

  static getField(key: string): Field | undefined {
    return PlaceBase.getSchemaFields().get(key);
  }

  queryFans(): PlaceToFansQuery {
    return PlaceToFansQuery.query(this.viewer, this.id);
  }

  queryReviews(): PlaceToReviewsQuery {
    return PlaceToReviewsQuery.query(this.viewer, this.id);
  }

  async loadCreator(): Promise<User | null> {
    return loadEnt(this.viewer, this.creatorId, User.loaderOptions());
  }

  loadCreatorX(): Promise<User> {
    return loadEntX(this.viewer, this.creatorId, User.loaderOptions());
  }
}
