// Generated by github.com/lolopinto/ent/ent, DO NOT EDIT.

import type {
  Context,
  CustomQuery,
  Data,
  Ent,
  ID,
  LoadEntOptions,
  PrivacyPolicy,
  Viewer,
} from "@snowtop/ent";
import type { Field } from "@snowtop/ent/schema";
import type { UserDBData } from "./loaders";
import {
  AllowIfViewerPrivacyPolicy,
  loadCustomCount,
  loadCustomData,
  loadCustomEnts,
  loadEnt,
  loadEntViaKey,
  loadEntX,
  loadEntXViaKey,
  loadEnts,
} from "@snowtop/ent";
import { getFields } from "@snowtop/ent/schema";
import { userLoader, userLoaderInfo, userSlugLoader } from "./loaders";
import { NodeType } from "./types";
import {
  UserToCreatedPlacesQuery,
  UserToFavoritePlacesQuery,
  UserToPlaceReviewsQuery,
} from "../internal";
import schema from "../../schema/user_schema";

export class UserBase implements Ent<Viewer> {
  protected readonly data: UserDBData;
  readonly nodeType = NodeType.User;
  readonly id: ID;
  readonly createdAt: Date;
  readonly updatedAt: Date;
  readonly name: string;
  readonly slug: string;
  readonly bio: string | null;

  constructor(
    public viewer: Viewer,
    data: Data,
  ) {
    this.id = data.id;
    this.createdAt = data.created_at;
    this.updatedAt = data.updated_at;
    this.name = data.name;
    this.slug = data.slug;
    this.bio = data.bio;
    // @ts-expect-error
    this.data = data;
  }

  __setRawDBData<UserDBData>(data: UserDBData) {}

  /** used by some ent internals to get access to raw db data. should not be depended on. may not always be on the ent **/
  ___getRawDBData(): UserDBData {
    return this.data;
  }

  getPrivacyPolicy(): PrivacyPolicy<this, Viewer> {
    return AllowIfViewerPrivacyPolicy;
  }

  static async load<T extends UserBase>(
    this: new (
      viewer: Viewer,
      data: Data,
    ) => T,
    viewer: Viewer,
    id: ID,
  ): Promise<T | null> {
    return (await loadEnt(
      viewer,
      id,
      UserBase.loaderOptions.apply(this),
    )) as T | null;
  }

  static async loadX<T extends UserBase>(
    this: new (
      viewer: Viewer,
      data: Data,
    ) => T,
    viewer: Viewer,
    id: ID,
  ): Promise<T> {
    return (await loadEntX(
      viewer,
      id,
      UserBase.loaderOptions.apply(this),
    )) as T;
  }

  static async loadMany<T extends UserBase>(
    this: new (
      viewer: Viewer,
      data: Data,
    ) => T,
    viewer: Viewer,
    ...ids: ID[]
  ): Promise<Map<ID, T>> {
    return (await loadEnts(
      viewer,
      UserBase.loaderOptions.apply(this),
      ...ids,
    )) as Map<ID, T>;
  }

  static async loadCustom<T extends UserBase>(
    this: new (
      viewer: Viewer,
      data: Data,
    ) => T,
    viewer: Viewer,
    query: CustomQuery<UserDBData>,
  ): Promise<T[]> {
    return (await loadCustomEnts(
      viewer,
      {
        ...UserBase.loaderOptions.apply(this),
        prime: true,
      },
      query,
    )) as T[];
  }

  static async loadCustomData<T extends UserBase>(
    this: new (
      viewer: Viewer,
      data: Data,
    ) => T,
    query: CustomQuery<UserDBData>,
    context?: Context,
  ): Promise<UserDBData[]> {
    return loadCustomData<UserDBData, UserDBData>(
      {
        ...UserBase.loaderOptions.apply(this),
        prime: true,
      },
      query,
      context,
    );
  }

  static async loadCustomCount<T extends UserBase>(
    this: new (
      viewer: Viewer,
      data: Data,
    ) => T,
    query: CustomQuery<UserDBData>,
    context?: Context,
  ): Promise<number> {
    return loadCustomCount(
      {
        ...UserBase.loaderOptions.apply(this),
      },
      query,
      context,
    );
  }

  static async loadRawData<T extends UserBase>(
    this: new (
      viewer: Viewer,
      data: Data,
    ) => T,
    id: ID,
    context?: Context,
  ): Promise<UserDBData | null> {
    return userLoader.createLoader(context).load(id);
  }

  static async loadRawDataX<T extends UserBase>(
    this: new (
      viewer: Viewer,
      data: Data,
    ) => T,
    id: ID,
    context?: Context,
  ): Promise<UserDBData> {
    const row = await userLoader.createLoader(context).load(id);
    if (!row) {
      throw new Error(`couldn't load row for ${id}`);
    }
    return row;
  }

  static async loadFromSlug<T extends UserBase>(
    this: new (
      viewer: Viewer,
      data: Data,
    ) => T,
    viewer: Viewer,
    slug: string,
  ): Promise<T | null> {
    return (await loadEntViaKey(viewer, slug, {
      ...UserBase.loaderOptions.apply(this),
      loaderFactory: userSlugLoader,
    })) as T | null;
  }

  static async loadFromSlugX<T extends UserBase>(
    this: new (
      viewer: Viewer,
      data: Data,
    ) => T,
    viewer: Viewer,
    slug: string,
  ): Promise<T> {
    return (await loadEntXViaKey(viewer, slug, {
      ...UserBase.loaderOptions.apply(this),
      loaderFactory: userSlugLoader,
    })) as T;
  }

  static async loadIdFromSlug<T extends UserBase>(
    this: new (
      viewer: Viewer,
      data: Data,
    ) => T,
    slug: string,
    context?: Context,
  ): Promise<ID | undefined> {
    const row = await userSlugLoader.createLoader(context).load(slug);
    return row?.id;
  }

  static async loadRawDataFromSlug<T extends UserBase>(
    this: new (
      viewer: Viewer,
      data: Data,
    ) => T,
    slug: string,
    context?: Context,
  ): Promise<UserDBData | null> {
    return userSlugLoader.createLoader(context).load(slug);
  }

  static loaderOptions<T extends UserBase>(
    this: new (
      viewer: Viewer,
      data: Data,
    ) => T,
  ): LoadEntOptions<T, Viewer, UserDBData> {
    return {
      tableName: userLoaderInfo.tableName,
      fields: userLoaderInfo.fields,
      ent: this,
      loaderFactory: userLoader,
    };
  }

  private static schemaFields: Map<string, Field>;

  private static getSchemaFields(): Map<string, Field> {
    if (UserBase.schemaFields != null) {
      return UserBase.schemaFields;
    }
    return (UserBase.schemaFields = getFields(schema));
  }

  static getField(key: string): Field | undefined {
    return UserBase.getSchemaFields().get(key);
  }

  queryCreatedPlaces(): UserToCreatedPlacesQuery {
    return UserToCreatedPlacesQuery.query(this.viewer, this.id);
  }

  queryFavoritePlaces(): UserToFavoritePlacesQuery {
    return UserToFavoritePlacesQuery.query(this.viewer, this.id);
  }

  queryPlaceReviews(): UserToPlaceReviewsQuery {
    return UserToPlaceReviewsQuery.query(this.viewer, this.id);
  }
}
