// Generated by github.com/lolopinto/ent/ent, DO NOT EDIT.

import type { Ent, ID, Viewer } from "@snowtop/ent";
import type {
  Action,
  Builder,
  Changeset,
  ChangesetOptions,
  OrchestratorOptions,
} from "@snowtop/ent/action";
import {
  Orchestrator,
  WriteOperation,
  saveBuilder,
  saveBuilderX,
} from "@snowtop/ent/action";
import { Place, PlaceReview, User } from "../../..";
import { placeReviewLoaderInfo } from "../../loaders";
import { EdgeType, NodeType } from "../../types";
import schema from "../../../../schema/place_review_schema";

export interface PlaceReviewInput {
  id?: ID;
  createdAt?: Date;
  updatedAt?: Date;
  placeId?: ID | Builder<Place, Viewer>;
  reviewerId?: ID | Builder<User, Viewer>;
  rating?: number;
  body?: string | null;
  // allow other properties. useful for action-only fields
  [x: string]: any;
}

function randomNum(): string {
  return Math.random().toString(10).substring(2);
}

type MaybeNull<T extends Ent> = T | null;
type TMaybleNullableEnt<T extends Ent> = T | MaybeNull<T>;

export class PlaceReviewBuilder<
  TInput extends PlaceReviewInput = PlaceReviewInput,
  TExistingEnt extends TMaybleNullableEnt<PlaceReview> = PlaceReview | null,
> implements Builder<PlaceReview, Viewer, TExistingEnt>
{
  orchestrator: Orchestrator<PlaceReview, TInput, Viewer, TExistingEnt>;
  readonly placeholderID: ID;
  readonly ent = PlaceReview;
  readonly nodeType = NodeType.PlaceReview;
  private input: TInput;
  // Values injected by the runtime remain readable without counting as edits.
  private defaultInput = new Map<string, any>();
  private m: Map<string, any> = new Map();

  public constructor(
    public readonly viewer: Viewer,
    public readonly operation: WriteOperation,
    action: Action<
      PlaceReview,
      Builder<PlaceReview, Viewer, TExistingEnt>,
      Viewer,
      TInput,
      TExistingEnt
    >,
    public readonly existingEnt: TExistingEnt,
    opts?: Partial<
      OrchestratorOptions<PlaceReview, TInput, Viewer, TExistingEnt>
    >,
  ) {
    this.placeholderID = `$ent.idPlaceholderID$ ${randomNum()}-PlaceReview`;
    this.input = action.getInput();
    const updateInput = (
      input: PlaceReviewInput,
      operation?: WriteOperation,
      defaultKeys?: ReadonlySet<string>,
    ) => {
      this.updateInput(input);
      for (const key of defaultKeys ?? []) {
        this.defaultInput.set(key, input[key]);
      }
    };

    this.orchestrator = new Orchestrator({
      viewer,
      operation: this.operation,
      tableName: "place_reviews",
      key: "id",
      loaderOptions: PlaceReview.loaderOptions(),
      builder: this,
      action,
      schema,
      editedFields: () => this.getEditedFields.apply(this),
      updateInput,
      fieldInfo: placeReviewLoaderInfo.fieldInfo,
      ...opts,
    });
  }

  getInput(): TInput {
    return this.input;
  }

  updateInput(input: PlaceReviewInput) {
    for (const key of Object.keys(input)) {
      this.defaultInput.delete(key);
    }
    // override input
    this.input = {
      ...this.input,
      ...input,
    };
  }

  deleteInputKey(key: keyof PlaceReviewInput) {
    this.defaultInput.delete(String(key));
    delete this.input[key];
  }

  // store data in Builder that can be retrieved by another validator, trigger, observer later in the action
  storeData(k: string, v: any) {
    this.m.set(k, v);
  }

  // retrieve data stored in this Builder with key
  getStoredData(k: string) {
    return this.m.get(k);
  }

  // this returns the id of the existing ent or the id of the ent that's being created
  async getEntID() {
    if (this.existingEnt) {
      return this.existingEnt.id;
    }
    const edited = await this.orchestrator.getEditedData();
    if (!edited.id) {
      throw new Error(
        `couldn't get the id field. should have been set by 'defaultValueOnCreate'`,
      );
    }
    return edited.id;
  }

  async build(): Promise<Changeset> {
    return this.orchestrator.build();
  }

  async buildWithOptions_BETA(options: ChangesetOptions): Promise<Changeset> {
    return this.orchestrator.buildWithOptions_BETA(options);
  }

  async valid(): Promise<boolean> {
    return this.orchestrator.valid();
  }

  async validX(): Promise<void> {
    return this.orchestrator.validX();
  }

  async save(): Promise<void> {
    await saveBuilder(this);
  }

  async saveX(): Promise<void> {
    await saveBuilderX(this);
  }

  async editedEnt(): Promise<PlaceReview | null> {
    return this.orchestrator.editedEnt();
  }

  async editedEntX(): Promise<PlaceReview> {
    return this.orchestrator.editedEntX();
  }

  private async getEditedFields(): Promise<Map<string, any>> {
    const input = this.input;

    const result = new Map<string, any>();

    const addField = (key: string, inputKey: string, value: any) => {
      if (
        value !== undefined &&
        (!this.defaultInput.has(inputKey) ||
          this.defaultInput.get(inputKey) !== value)
      ) {
        result.set(key, value);
      }
    };
    addField("id", "id", input.id);
    addField("createdAt", "createdAt", input.createdAt);
    addField("updatedAt", "updatedAt", input.updatedAt);
    addField("placeID", "placeId", input.placeId);
    addField("reviewerID", "reviewerId", input.reviewerId);
    addField("rating", "rating", input.rating);
    addField("body", "body", input.body);
    {
      const value = result.get("placeID");
      let existingIDs: ID[] = [];
      if (this.existingEnt) {
        const stored = this.existingEnt.placeId;
        existingIDs = stored == null ? [] : [stored];
      }
      this.orchestrator.__setFieldEdges(
        "placeID",
        value === undefined ? undefined : value === null ? [] : [value],
        EdgeType.PlaceToReviews,
        NodeType.Place,
        { existingIDs },
      );
    }
    {
      const value = result.get("reviewerID");
      let existingIDs: ID[] = [];
      if (this.existingEnt) {
        const stored = this.existingEnt.reviewerId;
        existingIDs = stored == null ? [] : [stored];
      }
      this.orchestrator.__setFieldEdges(
        "reviewerID",
        value === undefined ? undefined : value === null ? [] : [value],
        EdgeType.UserToPlaceReviews,
        NodeType.User,
        { existingIDs },
      );
    }
    return result;
  }

  isBuilder<T extends Ent>(
    node: ID | T | Builder<T, any>,
  ): node is Builder<T, any> {
    return (node as Builder<T, any>).placeholderID !== undefined;
  }

  // get value of placeID. Retrieves it from the input if specified or takes it from existingEnt
  getNewPlaceIdValue(): ID | Builder<Place, Viewer> {
    if (this.input.placeId !== undefined) {
      return this.input.placeId;
    }

    if (!this.existingEnt) {
      throw new Error(
        "no value to return for `placeId` since not in input and no existingEnt",
      );
    }
    return this.existingEnt.placeId;
  }

  // get value of reviewerID. Retrieves it from the input if specified or takes it from existingEnt
  getNewReviewerIdValue(): ID | Builder<User, Viewer> {
    if (this.input.reviewerId !== undefined) {
      return this.input.reviewerId;
    }

    if (!this.existingEnt) {
      throw new Error(
        "no value to return for `reviewerId` since not in input and no existingEnt",
      );
    }
    return this.existingEnt.reviewerId;
  }

  // get value of rating. Retrieves it from the input if specified or takes it from existingEnt
  getNewRatingValue(): number {
    if (this.input.rating !== undefined) {
      return this.input.rating;
    }

    if (!this.existingEnt) {
      throw new Error(
        "no value to return for `rating` since not in input and no existingEnt",
      );
    }
    return this.existingEnt.rating;
  }

  // get value of body. Retrieves it from the input if specified or takes it from existingEnt
  getNewBodyValue(): string | null {
    if (this.input.body !== undefined) {
      return this.input.body;
    }

    return this.existingEnt?.body ?? null;
  }
}
