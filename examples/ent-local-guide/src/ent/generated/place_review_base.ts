// Generated by github.com/lolopinto/ent/ent, DO NOT EDIT.

import type {
  Context,
  CustomQuery,
  Data,
  Ent,
  ID,
  LoadEntOptions,
  PrivacyPolicy,
  Viewer,
} from "@snowtop/ent";
import type { Field } from "@snowtop/ent/schema";
import type { PlaceReviewDBData } from "./loaders";
import {
  AllowIfViewerPrivacyPolicy,
  loadCustomCount,
  loadCustomData,
  loadCustomEnts,
  loadEnt,
  loadEntX,
  loadEnts,
} from "@snowtop/ent";
import { getFields } from "@snowtop/ent/schema";
import { placeReviewLoader, placeReviewLoaderInfo } from "./loaders";
import { NodeType } from "./types";
import { Place, User } from "../internal";
import schema from "../../schema/place_review_schema";

export class PlaceReviewBase implements Ent<Viewer> {
  protected readonly data: PlaceReviewDBData;
  readonly nodeType = NodeType.PlaceReview;
  readonly id: ID;
  readonly createdAt: Date;
  readonly updatedAt: Date;
  readonly placeId: ID;
  readonly reviewerId: ID;
  readonly rating: number;
  readonly body: string | null;

  constructor(
    public viewer: Viewer,
    data: Data,
  ) {
    this.id = data.id;
    this.createdAt = data.created_at;
    this.updatedAt = data.updated_at;
    this.placeId = data.place_id;
    this.reviewerId = data.reviewer_id;
    this.rating = data.rating;
    this.body = data.body;
    // @ts-expect-error
    this.data = data;
  }

  __setRawDBData<PlaceReviewDBData>(data: PlaceReviewDBData) {}

  /** used by some ent internals to get access to raw db data. should not be depended on. may not always be on the ent **/
  ___getRawDBData(): PlaceReviewDBData {
    return this.data;
  }

  getPrivacyPolicy(): PrivacyPolicy<this, Viewer> {
    return AllowIfViewerPrivacyPolicy;
  }

  static async load<T extends PlaceReviewBase>(
    this: new (
      viewer: Viewer,
      data: Data,
    ) => T,
    viewer: Viewer,
    id: ID,
  ): Promise<T | null> {
    return (await loadEnt(
      viewer,
      id,
      PlaceReviewBase.loaderOptions.apply(this),
    )) as T | null;
  }

  static async loadX<T extends PlaceReviewBase>(
    this: new (
      viewer: Viewer,
      data: Data,
    ) => T,
    viewer: Viewer,
    id: ID,
  ): Promise<T> {
    return (await loadEntX(
      viewer,
      id,
      PlaceReviewBase.loaderOptions.apply(this),
    )) as T;
  }

  static async loadMany<T extends PlaceReviewBase>(
    this: new (
      viewer: Viewer,
      data: Data,
    ) => T,
    viewer: Viewer,
    ...ids: ID[]
  ): Promise<Map<ID, T>> {
    return (await loadEnts(
      viewer,
      PlaceReviewBase.loaderOptions.apply(this),
      ...ids,
    )) as Map<ID, T>;
  }

  static async loadCustom<T extends PlaceReviewBase>(
    this: new (
      viewer: Viewer,
      data: Data,
    ) => T,
    viewer: Viewer,
    query: CustomQuery<PlaceReviewDBData>,
  ): Promise<T[]> {
    return (await loadCustomEnts(
      viewer,
      {
        ...PlaceReviewBase.loaderOptions.apply(this),
        prime: true,
      },
      query,
    )) as T[];
  }

  static async loadCustomData<T extends PlaceReviewBase>(
    this: new (
      viewer: Viewer,
      data: Data,
    ) => T,
    query: CustomQuery<PlaceReviewDBData>,
    context?: Context,
  ): Promise<PlaceReviewDBData[]> {
    return loadCustomData<PlaceReviewDBData, PlaceReviewDBData>(
      {
        ...PlaceReviewBase.loaderOptions.apply(this),
        prime: true,
      },
      query,
      context,
    );
  }

  static async loadCustomCount<T extends PlaceReviewBase>(
    this: new (
      viewer: Viewer,
      data: Data,
    ) => T,
    query: CustomQuery<PlaceReviewDBData>,
    context?: Context,
  ): Promise<number> {
    return loadCustomCount(
      {
        ...PlaceReviewBase.loaderOptions.apply(this),
      },
      query,
      context,
    );
  }

  static async loadRawData<T extends PlaceReviewBase>(
    this: new (
      viewer: Viewer,
      data: Data,
    ) => T,
    id: ID,
    context?: Context,
  ): Promise<PlaceReviewDBData | null> {
    return placeReviewLoader.createLoader(context).load(id);
  }

  static async loadRawDataX<T extends PlaceReviewBase>(
    this: new (
      viewer: Viewer,
      data: Data,
    ) => T,
    id: ID,
    context?: Context,
  ): Promise<PlaceReviewDBData> {
    const row = await placeReviewLoader.createLoader(context).load(id);
    if (!row) {
      throw new Error(`couldn't load row for ${id}`);
    }
    return row;
  }

  static loaderOptions<T extends PlaceReviewBase>(
    this: new (
      viewer: Viewer,
      data: Data,
    ) => T,
  ): LoadEntOptions<T, Viewer, PlaceReviewDBData> {
    return {
      tableName: placeReviewLoaderInfo.tableName,
      fields: placeReviewLoaderInfo.fields,
      ent: this,
      loaderFactory: placeReviewLoader,
    };
  }

  private static schemaFields: Map<string, Field>;

  private static getSchemaFields(): Map<string, Field> {
    if (PlaceReviewBase.schemaFields != null) {
      return PlaceReviewBase.schemaFields;
    }
    return (PlaceReviewBase.schemaFields = getFields(schema));
  }

  static getField(key: string): Field | undefined {
    return PlaceReviewBase.getSchemaFields().get(key);
  }

  async loadPlace(): Promise<Place | null> {
    return loadEnt(this.viewer, this.placeId, Place.loaderOptions());
  }

  loadPlaceX(): Promise<Place> {
    return loadEntX(this.viewer, this.placeId, Place.loaderOptions());
  }

  async loadReviewer(): Promise<User | null> {
    return loadEnt(this.viewer, this.reviewerId, User.loaderOptions());
  }

  loadReviewerX(): Promise<User> {
    return loadEntX(this.viewer, this.reviewerId, User.loaderOptions());
  }
}
