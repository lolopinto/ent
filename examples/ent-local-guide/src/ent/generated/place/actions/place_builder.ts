// Generated by github.com/lolopinto/ent/ent, DO NOT EDIT.

import type { AssocEdgeInputOptions, Ent, ID, Viewer } from "@snowtop/ent";
import type { GeoPoint } from "@snowtop/ent-postgis";
import type {
  Action,
  Builder,
  Changeset,
  ChangesetOptions,
  OrchestratorOptions,
} from "@snowtop/ent/action";
import {
  Orchestrator,
  WriteOperation,
  saveBuilder,
  saveBuilderX,
} from "@snowtop/ent/action";
import { Place, PlaceReview, User } from "../../..";
import { placeLoaderInfo } from "../../loaders";
import { EdgeType, NodeType, PlaceCategory } from "../../types";
import schema from "../../../../schema/place_schema";

export interface PlaceInput {
  name?: string;
  slug?: string;
  description?: string | null;
  website?: string | null;
  category?: PlaceCategory;
  creatorId?: ID | Builder<User, Viewer>;
  location?: GeoPoint;
  // allow other properties. useful for action-only fields
  [x: string]: any;
}

function randomNum(): string {
  return Math.random().toString(10).substring(2);
}

type MaybeNull<T extends Ent> = T | null;
type TMaybleNullableEnt<T extends Ent> = T | MaybeNull<T>;

export class PlaceBuilder<
  TInput extends PlaceInput = PlaceInput,
  TExistingEnt extends TMaybleNullableEnt<Place> = Place | null,
> implements Builder<Place, Viewer, TExistingEnt>
{
  orchestrator: Orchestrator<Place, TInput, Viewer, TExistingEnt>;
  readonly placeholderID: ID;
  readonly ent = Place;
  readonly nodeType = NodeType.Place;
  private input: TInput;
  private m: Map<string, any> = new Map();

  public constructor(
    public readonly viewer: Viewer,
    public readonly operation: WriteOperation,
    action: Action<
      Place,
      Builder<Place, Viewer, TExistingEnt>,
      Viewer,
      TInput,
      TExistingEnt
    >,
    public readonly existingEnt: TExistingEnt,
    opts?: Partial<OrchestratorOptions<Place, TInput, Viewer, TExistingEnt>>,
  ) {
    this.placeholderID = `$ent.idPlaceholderID$ ${randomNum()}-Place`;
    this.input = action.getInput();
    const updateInput = (d: PlaceInput) => this.updateInput.apply(this, [d]);

    this.orchestrator = new Orchestrator({
      viewer,
      operation: this.operation,
      tableName: "places",
      key: "id",
      loaderOptions: Place.loaderOptions(),
      builder: this,
      action,
      schema,
      editedFields: () => this.getEditedFields.apply(this),
      updateInput,
      fieldInfo: placeLoaderInfo.fieldInfo,
      ...opts,
    });
  }

  getInput(): TInput {
    return this.input;
  }

  updateInput(input: PlaceInput) {
    // override input
    this.input = {
      ...this.input,
      ...input,
    };
  }

  deleteInputKey(key: keyof PlaceInput) {
    delete this.input[key];
  }

  // store data in Builder that can be retrieved by another validator, trigger, observer later in the action
  storeData(k: string, v: any) {
    this.m.set(k, v);
  }

  // retrieve data stored in this Builder with key
  getStoredData(k: string) {
    return this.m.get(k);
  }

  // this returns the id of the existing ent or the id of the ent that's being created
  async getEntID() {
    if (this.existingEnt) {
      return this.existingEnt.id;
    }
    const edited = await this.orchestrator.getEditedData();
    if (!edited.id) {
      throw new Error(
        `couldn't get the id field. should have been set by 'defaultValueOnCreate'`,
      );
    }
    return edited.id;
  }
  // this gets the inputs that have been written for a given edgeType and operation
  // WriteOperation.Insert for adding an edge and WriteOperation.Delete for deleting an edge
  getEdgeInputData(edgeType: EdgeType, op: WriteOperation) {
    return this.orchestrator.getInputEdges(edgeType, op);
  }

  clearInputEdges(edgeType: EdgeType, op: WriteOperation, id?: ID) {
    this.orchestrator.clearInputEdges(edgeType, op, id);
  }

  addFan(...nodes: (ID | User | Builder<User, any>)[]): this {
    for (const node of nodes) {
      if (this.isBuilder(node)) {
        this.addFanID(node);
      } else if (typeof node === "object") {
        this.addFanID(node.id);
      } else {
        this.addFanID(node);
      }
    }
    return this;
  }

  addFanID(id: ID | Builder<User, any>, options?: AssocEdgeInputOptions): this {
    this.orchestrator.addOutboundEdge(
      id,
      EdgeType.PlaceToFans,
      NodeType.User,
      options,
    );
    return this;
  }

  removeFan(...nodes: (ID | User)[]): this {
    for (const node of nodes) {
      if (typeof node === "object") {
        this.orchestrator.removeOutboundEdge(node.id, EdgeType.PlaceToFans);
      } else {
        this.orchestrator.removeOutboundEdge(node, EdgeType.PlaceToFans);
      }
    }
    return this;
  }

  addReview(...nodes: (ID | PlaceReview | Builder<PlaceReview, any>)[]): this {
    for (const node of nodes) {
      if (this.isBuilder(node)) {
        this.addReviewID(node);
      } else if (typeof node === "object") {
        this.addReviewID(node.id);
      } else {
        this.addReviewID(node);
      }
    }
    return this;
  }

  addReviewID(
    id: ID | Builder<PlaceReview, any>,
    options?: AssocEdgeInputOptions,
  ): this {
    this.orchestrator.addOutboundEdge(
      id,
      EdgeType.PlaceToReviews,
      NodeType.PlaceReview,
      options,
    );
    return this;
  }

  removeReview(...nodes: (ID | PlaceReview)[]): this {
    for (const node of nodes) {
      if (typeof node === "object") {
        this.orchestrator.removeOutboundEdge(node.id, EdgeType.PlaceToReviews);
      } else {
        this.orchestrator.removeOutboundEdge(node, EdgeType.PlaceToReviews);
      }
    }
    return this;
  }

  async build(): Promise<Changeset> {
    return this.orchestrator.build();
  }

  async buildWithOptions_BETA(options: ChangesetOptions): Promise<Changeset> {
    return this.orchestrator.buildWithOptions_BETA(options);
  }

  async valid(): Promise<boolean> {
    return this.orchestrator.valid();
  }

  async validX(): Promise<void> {
    return this.orchestrator.validX();
  }

  async save(): Promise<void> {
    await saveBuilder(this);
  }

  async saveX(): Promise<void> {
    await saveBuilderX(this);
  }

  async editedEnt(): Promise<Place | null> {
    return this.orchestrator.editedEnt();
  }

  async editedEntX(): Promise<Place> {
    return this.orchestrator.editedEntX();
  }

  private async getEditedFields(): Promise<Map<string, any>> {
    const input = this.input;

    const result = new Map<string, any>();

    const addField = function (key: string, value: any) {
      if (value !== undefined) {
        result.set(key, value);
      }
    };
    addField("name", input.name);
    addField("slug", input.slug);
    addField("description", input.description);
    addField("website", input.website);
    addField("category", input.category);
    addField("creatorID", input.creatorId);
    if (
      input.creatorId !== undefined ||
      this.operation === WriteOperation.Delete
    ) {
      if (input.creatorId) {
        this.orchestrator.addInboundEdge(
          input.creatorId,
          EdgeType.UserToCreatedPlaces,
          NodeType.User,
        );
      }
      if (
        this.existingEnt &&
        this.existingEnt.creatorId &&
        this.existingEnt.creatorId !== input.creatorId
      ) {
        this.orchestrator.removeInboundEdge(
          this.existingEnt.creatorId,
          EdgeType.UserToCreatedPlaces,
        );
      }
    }
    addField("location", input.location);
    return result;
  }

  isBuilder<T extends Ent>(
    node: ID | T | Builder<T, any>,
  ): node is Builder<T, any> {
    return (node as Builder<T, any>).placeholderID !== undefined;
  }

  // get value of name. Retrieves it from the input if specified or takes it from existingEnt
  getNewNameValue(): string {
    if (this.input.name !== undefined) {
      return this.input.name;
    }

    if (!this.existingEnt) {
      throw new Error(
        "no value to return for `name` since not in input and no existingEnt",
      );
    }
    return this.existingEnt.name;
  }

  // get value of slug. Retrieves it from the input if specified or takes it from existingEnt
  getNewSlugValue(): string {
    if (this.input.slug !== undefined) {
      return this.input.slug;
    }

    if (!this.existingEnt) {
      throw new Error(
        "no value to return for `slug` since not in input and no existingEnt",
      );
    }
    return this.existingEnt.slug;
  }

  // get value of description. Retrieves it from the input if specified or takes it from existingEnt
  getNewDescriptionValue(): string | null {
    if (this.input.description !== undefined) {
      return this.input.description;
    }

    return this.existingEnt?.description ?? null;
  }

  // get value of website. Retrieves it from the input if specified or takes it from existingEnt
  getNewWebsiteValue(): string | null {
    if (this.input.website !== undefined) {
      return this.input.website;
    }

    return this.existingEnt?.website ?? null;
  }

  // get value of category. Retrieves it from the input if specified or takes it from existingEnt
  getNewCategoryValue(): PlaceCategory {
    if (this.input.category !== undefined) {
      return this.input.category;
    }

    if (!this.existingEnt) {
      throw new Error(
        "no value to return for `category` since not in input and no existingEnt",
      );
    }
    return this.existingEnt.category;
  }

  // get value of creatorID. Retrieves it from the input if specified or takes it from existingEnt
  getNewCreatorIdValue(): ID | Builder<User, Viewer> {
    if (this.input.creatorId !== undefined) {
      return this.input.creatorId;
    }

    if (!this.existingEnt) {
      throw new Error(
        "no value to return for `creatorId` since not in input and no existingEnt",
      );
    }
    return this.existingEnt.creatorId;
  }

  // get value of location. Retrieves it from the input if specified or takes it from existingEnt
  getNewLocationValue(): GeoPoint {
    if (this.input.location !== undefined) {
      return this.input.location;
    }

    if (!this.existingEnt) {
      throw new Error(
        "no value to return for `location` since not in input and no existingEnt",
      );
    }
    return this.existingEnt.location;
  }
}
