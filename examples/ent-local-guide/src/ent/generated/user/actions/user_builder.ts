// Generated by github.com/lolopinto/ent/ent, DO NOT EDIT.

import type { AssocEdgeInputOptions, Ent, ID, Viewer } from "@snowtop/ent";
import type {
  Action,
  Builder,
  Changeset,
  ChangesetOptions,
  OrchestratorOptions,
} from "@snowtop/ent/action";
import {
  Orchestrator,
  WriteOperation,
  saveBuilder,
  saveBuilderX,
} from "@snowtop/ent/action";
import { Place, PlaceReview, User } from "../../..";
import { userLoaderInfo } from "../../loaders";
import { EdgeType, NodeType } from "../../types";
import schema from "../../../../schema/user_schema";

export interface UserInput {
  name?: string;
  slug?: string;
  bio?: string | null;
  // allow other properties. useful for action-only fields
  [x: string]: any;
}

function randomNum(): string {
  return Math.random().toString(10).substring(2);
}

type MaybeNull<T extends Ent> = T | null;
type TMaybleNullableEnt<T extends Ent> = T | MaybeNull<T>;

export class UserBuilder<
  TInput extends UserInput = UserInput,
  TExistingEnt extends TMaybleNullableEnt<User> = User | null,
> implements Builder<User, Viewer, TExistingEnt>
{
  orchestrator: Orchestrator<User, TInput, Viewer, TExistingEnt>;
  readonly placeholderID: ID;
  readonly ent = User;
  readonly nodeType = NodeType.User;
  private input: TInput;
  private m: Map<string, any> = new Map();

  public constructor(
    public readonly viewer: Viewer,
    public readonly operation: WriteOperation,
    action: Action<
      User,
      Builder<User, Viewer, TExistingEnt>,
      Viewer,
      TInput,
      TExistingEnt
    >,
    public readonly existingEnt: TExistingEnt,
    opts?: Partial<OrchestratorOptions<User, TInput, Viewer, TExistingEnt>>,
  ) {
    this.placeholderID = `$ent.idPlaceholderID$ ${randomNum()}-User`;
    this.input = action.getInput();
    const updateInput = (d: UserInput) => this.updateInput.apply(this, [d]);

    this.orchestrator = new Orchestrator({
      viewer,
      operation: this.operation,
      tableName: "users",
      key: "id",
      loaderOptions: User.loaderOptions(),
      builder: this,
      action,
      schema,
      editedFields: () => this.getEditedFields.apply(this),
      updateInput,
      fieldInfo: userLoaderInfo.fieldInfo,
      ...opts,
    });
  }

  getInput(): TInput {
    return this.input;
  }

  updateInput(input: UserInput) {
    // override input
    this.input = {
      ...this.input,
      ...input,
    };
  }

  deleteInputKey(key: keyof UserInput) {
    delete this.input[key];
  }

  // store data in Builder that can be retrieved by another validator, trigger, observer later in the action
  storeData(k: string, v: any) {
    this.m.set(k, v);
  }

  // retrieve data stored in this Builder with key
  getStoredData(k: string) {
    return this.m.get(k);
  }

  // this returns the id of the existing ent or the id of the ent that's being created
  async getEntID() {
    if (this.existingEnt) {
      return this.existingEnt.id;
    }
    const edited = await this.orchestrator.getEditedData();
    if (!edited.id) {
      throw new Error(
        `couldn't get the id field. should have been set by 'defaultValueOnCreate'`,
      );
    }
    return edited.id;
  }
  // this gets the inputs that have been written for a given edgeType and operation
  // WriteOperation.Insert for adding an edge and WriteOperation.Delete for deleting an edge
  getEdgeInputData(edgeType: EdgeType, op: WriteOperation) {
    return this.orchestrator.getInputEdges(edgeType, op);
  }

  clearInputEdges(edgeType: EdgeType, op: WriteOperation, id?: ID) {
    this.orchestrator.clearInputEdges(edgeType, op, id);
  }

  addCreatedPlace(...nodes: (ID | Place | Builder<Place, any>)[]): this {
    for (const node of nodes) {
      if (this.isBuilder(node)) {
        this.addCreatedPlaceID(node);
      } else if (typeof node === "object") {
        this.addCreatedPlaceID(node.id);
      } else {
        this.addCreatedPlaceID(node);
      }
    }
    return this;
  }

  addCreatedPlaceID(
    id: ID | Builder<Place, any>,
    options?: AssocEdgeInputOptions,
  ): this {
    this.orchestrator.addOutboundEdge(
      id,
      EdgeType.UserToCreatedPlaces,
      NodeType.Place,
      options,
    );
    return this;
  }

  removeCreatedPlace(...nodes: (ID | Place)[]): this {
    for (const node of nodes) {
      if (typeof node === "object") {
        this.orchestrator.removeOutboundEdge(
          node.id,
          EdgeType.UserToCreatedPlaces,
        );
      } else {
        this.orchestrator.removeOutboundEdge(
          node,
          EdgeType.UserToCreatedPlaces,
        );
      }
    }
    return this;
  }

  addFavoritePlace(...nodes: (ID | Place | Builder<Place, any>)[]): this {
    for (const node of nodes) {
      if (this.isBuilder(node)) {
        this.addFavoritePlaceID(node);
      } else if (typeof node === "object") {
        this.addFavoritePlaceID(node.id);
      } else {
        this.addFavoritePlaceID(node);
      }
    }
    return this;
  }

  addFavoritePlaceID(
    id: ID | Builder<Place, any>,
    options?: AssocEdgeInputOptions,
  ): this {
    this.orchestrator.addOutboundEdge(
      id,
      EdgeType.UserToFavoritePlaces,
      NodeType.Place,
      options,
    );
    return this;
  }

  removeFavoritePlace(...nodes: (ID | Place)[]): this {
    for (const node of nodes) {
      if (typeof node === "object") {
        this.orchestrator.removeOutboundEdge(
          node.id,
          EdgeType.UserToFavoritePlaces,
        );
      } else {
        this.orchestrator.removeOutboundEdge(
          node,
          EdgeType.UserToFavoritePlaces,
        );
      }
    }
    return this;
  }

  addPlaceReview(
    ...nodes: (ID | PlaceReview | Builder<PlaceReview, any>)[]
  ): this {
    for (const node of nodes) {
      if (this.isBuilder(node)) {
        this.addPlaceReviewID(node);
      } else if (typeof node === "object") {
        this.addPlaceReviewID(node.id);
      } else {
        this.addPlaceReviewID(node);
      }
    }
    return this;
  }

  addPlaceReviewID(
    id: ID | Builder<PlaceReview, any>,
    options?: AssocEdgeInputOptions,
  ): this {
    this.orchestrator.addOutboundEdge(
      id,
      EdgeType.UserToPlaceReviews,
      NodeType.PlaceReview,
      options,
    );
    return this;
  }

  removePlaceReview(...nodes: (ID | PlaceReview)[]): this {
    for (const node of nodes) {
      if (typeof node === "object") {
        this.orchestrator.removeOutboundEdge(
          node.id,
          EdgeType.UserToPlaceReviews,
        );
      } else {
        this.orchestrator.removeOutboundEdge(node, EdgeType.UserToPlaceReviews);
      }
    }
    return this;
  }

  async build(): Promise<Changeset> {
    return this.orchestrator.build();
  }

  async buildWithOptions_BETA(options: ChangesetOptions): Promise<Changeset> {
    return this.orchestrator.buildWithOptions_BETA(options);
  }

  async valid(): Promise<boolean> {
    return this.orchestrator.valid();
  }

  async validX(): Promise<void> {
    return this.orchestrator.validX();
  }

  async save(): Promise<void> {
    await saveBuilder(this);
  }

  async saveX(): Promise<void> {
    await saveBuilderX(this);
  }

  async editedEnt(): Promise<User | null> {
    return this.orchestrator.editedEnt();
  }

  async editedEntX(): Promise<User> {
    return this.orchestrator.editedEntX();
  }

  private async getEditedFields(): Promise<Map<string, any>> {
    const input = this.input;

    const result = new Map<string, any>();

    const addField = function (key: string, value: any) {
      if (value !== undefined) {
        result.set(key, value);
      }
    };
    addField("name", input.name);
    addField("slug", input.slug);
    addField("bio", input.bio);
    return result;
  }

  isBuilder<T extends Ent>(
    node: ID | T | Builder<T, any>,
  ): node is Builder<T, any> {
    return (node as Builder<T, any>).placeholderID !== undefined;
  }

  // get value of name. Retrieves it from the input if specified or takes it from existingEnt
  getNewNameValue(): string {
    if (this.input.name !== undefined) {
      return this.input.name;
    }

    if (!this.existingEnt) {
      throw new Error(
        "no value to return for `name` since not in input and no existingEnt",
      );
    }
    return this.existingEnt.name;
  }

  // get value of slug. Retrieves it from the input if specified or takes it from existingEnt
  getNewSlugValue(): string {
    if (this.input.slug !== undefined) {
      return this.input.slug;
    }

    if (!this.existingEnt) {
      throw new Error(
        "no value to return for `slug` since not in input and no existingEnt",
      );
    }
    return this.existingEnt.slug;
  }

  // get value of bio. Retrieves it from the input if specified or takes it from existingEnt
  getNewBioValue(): string | null {
    if (this.input.bio !== undefined) {
      return this.input.bio;
    }

    return this.existingEnt?.bio ?? null;
  }
}
