// Generated by github.com/lolopinto/ent/ent, DO NOT EDIT.

import type { AssocEdgeInputOptions, Ent, ID, Viewer } from "@snowtop/ent";
import type {
  Action,
  Builder,
  Changeset,
  ChangesetOptions,
  OrchestratorOptions,
} from "@snowtop/ent/action";
import {
  Orchestrator,
  WriteOperation,
  saveBuilder,
  saveBuilderX,
} from "@snowtop/ent/action";
import { Note, NoteChunk, Tag, User, Workspace } from "../../..";
import { noteLoaderInfo } from "../../loaders";
import { EdgeType, NodeType, NoteStatus } from "../../types";
import schema from "../../../../schema/note_schema";

export interface NoteInput {
  workspaceId?: ID | Builder<Workspace, Viewer>;
  authorId?: ID | Builder<User, Viewer>;
  title?: string;
  body?: string;
  summary?: string | null;
  status?: NoteStatus;
  // allow other properties. useful for action-only fields
  [x: string]: any;
}

function randomNum(): string {
  return Math.random().toString(10).substring(2);
}

type MaybeNull<T extends Ent> = T | null;
type TMaybleNullableEnt<T extends Ent> = T | MaybeNull<T>;

export class NoteBuilder<
  TInput extends NoteInput = NoteInput,
  TExistingEnt extends TMaybleNullableEnt<Note> = Note | null,
> implements Builder<Note, Viewer, TExistingEnt>
{
  orchestrator: Orchestrator<Note, TInput, Viewer, TExistingEnt>;
  readonly placeholderID: ID;
  readonly ent = Note;
  readonly nodeType = NodeType.Note;
  private input: TInput;
  private m: Map<string, any> = new Map();

  public constructor(
    public readonly viewer: Viewer,
    public readonly operation: WriteOperation,
    action: Action<
      Note,
      Builder<Note, Viewer, TExistingEnt>,
      Viewer,
      TInput,
      TExistingEnt
    >,
    public readonly existingEnt: TExistingEnt,
    opts?: Partial<OrchestratorOptions<Note, TInput, Viewer, TExistingEnt>>,
  ) {
    this.placeholderID = `$ent.idPlaceholderID$ ${randomNum()}-Note`;
    this.input = action.getInput();
    const updateInput = (d: NoteInput) => this.updateInput.apply(this, [d]);

    this.orchestrator = new Orchestrator({
      viewer,
      operation: this.operation,
      tableName: "notes",
      key: "id",
      loaderOptions: Note.loaderOptions(),
      builder: this,
      action,
      schema,
      editedFields: () => this.getEditedFields.apply(this),
      updateInput,
      fieldInfo: noteLoaderInfo.fieldInfo,
      ...opts,
    });
  }

  getInput(): TInput {
    return this.input;
  }

  updateInput(input: Omit<NoteInput, "workspaceId" | "authorId">) {
    if (input.workspaceId !== undefined) {
      throw new Error(
        `workspaceId cannot be passed to updateInput. use overrideWorkspaceId instead`,
      );
    }

    if (input.authorId !== undefined) {
      throw new Error(
        `authorId cannot be passed to updateInput. use overrideAuthorId instead`,
      );
    }

    // override input
    this.input = {
      ...this.input,
      ...input,
    };
  }

  // override immutable field `workspaceId`
  overrideWorkspaceId(val: ID | Builder<Workspace, Viewer>) {
    this.input.workspaceId = val;
  }

  // override immutable field `authorId`
  overrideAuthorId(val: ID | Builder<User, Viewer>) {
    this.input.authorId = val;
  }

  deleteInputKey(key: keyof NoteInput) {
    delete this.input[key];
  }

  // store data in Builder that can be retrieved by another validator, trigger, observer later in the action
  storeData(k: string, v: any) {
    this.m.set(k, v);
  }

  // retrieve data stored in this Builder with key
  getStoredData(k: string) {
    return this.m.get(k);
  }

  // this returns the id of the existing ent or the id of the ent that's being created
  async getEntID() {
    if (this.existingEnt) {
      return this.existingEnt.id;
    }
    const edited = await this.orchestrator.getEditedData();
    if (!edited.id) {
      throw new Error(
        `couldn't get the id field. should have been set by 'defaultValueOnCreate'`,
      );
    }
    return edited.id;
  }
  // this gets the inputs that have been written for a given edgeType and operation
  // WriteOperation.Insert for adding an edge and WriteOperation.Delete for deleting an edge
  getEdgeInputData(edgeType: EdgeType, op: WriteOperation) {
    return this.orchestrator.getInputEdges(edgeType, op);
  }

  clearInputEdges(edgeType: EdgeType, op: WriteOperation, id?: ID) {
    this.orchestrator.clearInputEdges(edgeType, op, id);
  }

  addChunk(...nodes: (ID | NoteChunk | Builder<NoteChunk, any>)[]): this {
    for (const node of nodes) {
      if (this.isBuilder(node)) {
        this.addChunkID(node);
      } else if (typeof node === "object") {
        this.addChunkID(node.id);
      } else {
        this.addChunkID(node);
      }
    }
    return this;
  }

  addChunkID(
    id: ID | Builder<NoteChunk, any>,
    options?: AssocEdgeInputOptions,
  ): this {
    this.orchestrator.addOutboundEdge(
      id,
      EdgeType.NoteToChunks,
      NodeType.NoteChunk,
      options,
    );
    return this;
  }

  removeChunk(...nodes: (ID | NoteChunk)[]): this {
    for (const node of nodes) {
      if (typeof node === "object") {
        this.orchestrator.removeOutboundEdge(node.id, EdgeType.NoteToChunks);
      } else {
        this.orchestrator.removeOutboundEdge(node, EdgeType.NoteToChunks);
      }
    }
    return this;
  }

  addSavedBy(...nodes: (ID | User | Builder<User, any>)[]): this {
    for (const node of nodes) {
      if (this.isBuilder(node)) {
        this.addSavedByID(node);
      } else if (typeof node === "object") {
        this.addSavedByID(node.id);
      } else {
        this.addSavedByID(node);
      }
    }
    return this;
  }

  addSavedByID(
    id: ID | Builder<User, any>,
    options?: AssocEdgeInputOptions,
  ): this {
    this.orchestrator.addOutboundEdge(
      id,
      EdgeType.NoteToSavedBy,
      NodeType.User,
      options,
    );
    return this;
  }

  removeSavedBy(...nodes: (ID | User)[]): this {
    for (const node of nodes) {
      if (typeof node === "object") {
        this.orchestrator.removeOutboundEdge(node.id, EdgeType.NoteToSavedBy);
      } else {
        this.orchestrator.removeOutboundEdge(node, EdgeType.NoteToSavedBy);
      }
    }
    return this;
  }

  addTag(...nodes: (ID | Tag | Builder<Tag, any>)[]): this {
    for (const node of nodes) {
      if (this.isBuilder(node)) {
        this.addTagID(node);
      } else if (typeof node === "object") {
        this.addTagID(node.id);
      } else {
        this.addTagID(node);
      }
    }
    return this;
  }

  addTagID(id: ID | Builder<Tag, any>, options?: AssocEdgeInputOptions): this {
    this.orchestrator.addOutboundEdge(
      id,
      EdgeType.NoteToTags,
      NodeType.Tag,
      options,
    );
    return this;
  }

  removeTag(...nodes: (ID | Tag)[]): this {
    for (const node of nodes) {
      if (typeof node === "object") {
        this.orchestrator.removeOutboundEdge(node.id, EdgeType.NoteToTags);
      } else {
        this.orchestrator.removeOutboundEdge(node, EdgeType.NoteToTags);
      }
    }
    return this;
  }

  async build(): Promise<Changeset> {
    return this.orchestrator.build();
  }

  async buildWithOptions_BETA(options: ChangesetOptions): Promise<Changeset> {
    return this.orchestrator.buildWithOptions_BETA(options);
  }

  async valid(): Promise<boolean> {
    return this.orchestrator.valid();
  }

  async validX(): Promise<void> {
    return this.orchestrator.validX();
  }

  async save(): Promise<void> {
    await saveBuilder(this);
  }

  async saveX(): Promise<void> {
    await saveBuilderX(this);
  }

  async editedEnt(): Promise<Note | null> {
    return this.orchestrator.editedEnt();
  }

  async editedEntX(): Promise<Note> {
    return this.orchestrator.editedEntX();
  }

  private async getEditedFields(): Promise<Map<string, any>> {
    const input = this.input;

    const result = new Map<string, any>();

    const addField = function (key: string, value: any) {
      if (value !== undefined) {
        result.set(key, value);
      }
    };
    addField("workspaceID", input.workspaceId);
    if (
      input.workspaceId !== undefined ||
      this.operation === WriteOperation.Delete
    ) {
      if (input.workspaceId) {
        this.orchestrator.addInboundEdge(
          input.workspaceId,
          EdgeType.WorkspaceToNotes,
          NodeType.Workspace,
        );
      }
      if (
        this.existingEnt &&
        this.existingEnt.workspaceId &&
        this.existingEnt.workspaceId !== input.workspaceId
      ) {
        this.orchestrator.removeInboundEdge(
          this.existingEnt.workspaceId,
          EdgeType.WorkspaceToNotes,
        );
      }
    }
    addField("authorID", input.authorId);
    if (
      input.authorId !== undefined ||
      this.operation === WriteOperation.Delete
    ) {
      if (input.authorId) {
        this.orchestrator.addInboundEdge(
          input.authorId,
          EdgeType.UserToNotesAuthored,
          NodeType.User,
        );
      }
      if (
        this.existingEnt &&
        this.existingEnt.authorId &&
        this.existingEnt.authorId !== input.authorId
      ) {
        this.orchestrator.removeInboundEdge(
          this.existingEnt.authorId,
          EdgeType.UserToNotesAuthored,
        );
      }
    }
    addField("title", input.title);
    addField("body", input.body);
    addField("summary", input.summary);
    addField("status", input.status);
    return result;
  }

  isBuilder<T extends Ent>(
    node: ID | T | Builder<T, any>,
  ): node is Builder<T, any> {
    return (node as Builder<T, any>).placeholderID !== undefined;
  }

  // get value of workspaceID. Retrieves it from the input if specified or takes it from existingEnt
  getNewWorkspaceIdValue(): ID | Builder<Workspace, Viewer> {
    if (this.input.workspaceId !== undefined) {
      return this.input.workspaceId;
    }

    if (!this.existingEnt) {
      throw new Error(
        "no value to return for `workspaceId` since not in input and no existingEnt",
      );
    }
    return this.existingEnt.workspaceId;
  }

  // get value of authorID. Retrieves it from the input if specified or takes it from existingEnt
  getNewAuthorIdValue(): ID | Builder<User, Viewer> {
    if (this.input.authorId !== undefined) {
      return this.input.authorId;
    }

    if (!this.existingEnt) {
      throw new Error(
        "no value to return for `authorId` since not in input and no existingEnt",
      );
    }
    return this.existingEnt.authorId;
  }

  // get value of title. Retrieves it from the input if specified or takes it from existingEnt
  getNewTitleValue(): string {
    if (this.input.title !== undefined) {
      return this.input.title;
    }

    if (!this.existingEnt) {
      throw new Error(
        "no value to return for `title` since not in input and no existingEnt",
      );
    }
    return this.existingEnt.title;
  }

  // get value of body. Retrieves it from the input if specified or takes it from existingEnt
  getNewBodyValue(): string {
    if (this.input.body !== undefined) {
      return this.input.body;
    }

    if (!this.existingEnt) {
      throw new Error(
        "no value to return for `body` since not in input and no existingEnt",
      );
    }
    return this.existingEnt.body;
  }

  // get value of summary. Retrieves it from the input if specified or takes it from existingEnt
  getNewSummaryValue(): string | null {
    if (this.input.summary !== undefined) {
      return this.input.summary;
    }

    return this.existingEnt?.summary ?? null;
  }

  // get value of status. Retrieves it from the input if specified or takes it from existingEnt
  getNewStatusValue(): NoteStatus {
    if (this.input.status !== undefined) {
      return this.input.status;
    }

    if (!this.existingEnt) {
      throw new Error(
        "no value to return for `status` since not in input and no existingEnt",
      );
    }
    return this.existingEnt.status;
  }
}
