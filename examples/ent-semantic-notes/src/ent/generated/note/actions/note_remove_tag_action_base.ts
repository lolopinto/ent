// Generated by github.com/lolopinto/ent/ent, DO NOT EDIT.

import type { ID, PrivacyPolicy, Viewer } from "@snowtop/ent";
import type {
  Action,
  Changeset,
  ChangesetOptions,
  Observer,
  Trigger,
  Validator,
} from "@snowtop/ent/action";
import type { NoteInput } from "./note_builder";
import { AllowIfViewerHasIdentityPrivacyPolicy } from "@snowtop/ent";
import { WriteOperation } from "@snowtop/ent/action";
import { Note, Tag } from "../../..";
import { NoteBuilder } from "./note_builder";

export type NoteRemoveTagActionTriggers = (
  | Trigger<Note, NoteBuilder<NoteInput, Note>, Viewer, NoteInput, Note>
  | Trigger<Note, NoteBuilder<NoteInput, Note>, Viewer, NoteInput, Note>[]
)[];

export type NoteRemoveTagActionObservers = Observer<
  Note,
  NoteBuilder<NoteInput, Note>,
  Viewer,
  NoteInput,
  Note
>[];

export type NoteRemoveTagActionValidators = Validator<
  Note,
  NoteBuilder<NoteInput, Note>,
  Viewer,
  NoteInput,
  Note
>[];

export class NoteRemoveTagActionBase
  implements Action<Note, NoteBuilder<NoteInput, Note>, Viewer, NoteInput, Note>
{
  public readonly builder: NoteBuilder<NoteInput, Note>;
  public readonly viewer: Viewer;
  protected readonly note: Note;

  constructor(viewer: Viewer, note: Note) {
    this.viewer = viewer;
    this.builder = new NoteBuilder(
      this.viewer,
      WriteOperation.Edit,
      this,
      note,
    );
    this.note = note;
  }

  getPrivacyPolicy(): PrivacyPolicy<Note, Viewer> {
    return AllowIfViewerHasIdentityPrivacyPolicy;
  }

  getTriggers(): NoteRemoveTagActionTriggers {
    return [];
  }

  getObservers(): NoteRemoveTagActionObservers {
    return [];
  }

  getValidators(): NoteRemoveTagActionValidators {
    return [];
  }

  getInput(): NoteInput {
    return {};
  }

  removeTag(...nodes: (ID | Tag)[]): this {
    nodes.forEach((node) => this.builder.removeTag(node));
    return this;
  }
  async changeset(): Promise<Changeset> {
    return this.builder.build();
  }

  async changesetWithOptions_BETA(
    options: ChangesetOptions,
  ): Promise<Changeset> {
    return this.builder.buildWithOptions_BETA(options);
  }

  async valid(): Promise<boolean> {
    return this.builder.valid();
  }

  async validX(): Promise<void> {
    await this.builder.validX();
  }

  async save(): Promise<Note | null> {
    await this.builder.save();
    return this.builder.editedEnt();
  }

  async saveX(): Promise<Note> {
    await this.builder.saveX();
    return this.builder.editedEntX();
  }

  static create<T extends NoteRemoveTagActionBase>(
    this: new (
      viewer: Viewer,
      note: Note,
    ) => T,
    viewer: Viewer,
    note: Note,
  ): T {
    return new this(viewer, note);
  }

  static async saveXFromID<T extends NoteRemoveTagActionBase>(
    this: new (
      viewer: Viewer,
      note: Note,
    ) => T,
    viewer: Viewer,
    id: ID,
    tagId: ID,
  ): Promise<Note> {
    const note = await Note.loadX(viewer, id);
    return new this(viewer, note).removeTag(tagId).saveX();
  }
}
