// Generated by github.com/lolopinto/ent/ent, DO NOT EDIT.

import type { EdgeQuerySource, ID, OrderBy, Viewer } from "@snowtop/ent";
import {
  AssocEdgeCountLoaderFactory,
  AssocEdgeLoaderFactory,
  AssocEdgeQueryBase,
  CustomEdgeQueryBase,
} from "@snowtop/ent";
import { EdgeType } from "./types";
import {
  Note,
  NoteChunk,
  NoteToChunksEdge,
  NoteToSavedByEdge,
  NoteToTagsEdge,
  Tag,
  TagToNotesQuery,
  User,
  UserBase,
  UserToCreatedWorkspacesQuery,
  UserToNotesAuthoredQuery,
  UserToSavedNotesQuery,
  UserToWorkspacesQuery,
  WorkspaceBase,
} from "../internal";

export const noteToChunksCountLoaderFactory = new AssocEdgeCountLoaderFactory(
  EdgeType.NoteToChunks,
);
export const noteToChunksDataLoaderFactory = new AssocEdgeLoaderFactory(
  EdgeType.NoteToChunks,
  () => NoteToChunksEdge,
);

export const noteToSavedByCountLoaderFactory = new AssocEdgeCountLoaderFactory(
  EdgeType.NoteToSavedBy,
);
export const noteToSavedByDataLoaderFactory = new AssocEdgeLoaderFactory(
  EdgeType.NoteToSavedBy,
  () => NoteToSavedByEdge,
);

export const noteToTagsCountLoaderFactory = new AssocEdgeCountLoaderFactory(
  EdgeType.NoteToTags,
);
export const noteToTagsDataLoaderFactory = new AssocEdgeLoaderFactory(
  EdgeType.NoteToTags,
  () => NoteToTagsEdge,
);

export abstract class NoteToChunksQueryBase extends AssocEdgeQueryBase<
  Note,
  NoteChunk,
  NoteToChunksEdge,
  Viewer
> {
  constructor(viewer: Viewer, src: EdgeQuerySource<Note, NoteChunk, Viewer>) {
    super(
      viewer,
      src,
      noteToChunksCountLoaderFactory,
      noteToChunksDataLoaderFactory,
      NoteChunk.loaderOptions(),
    );
  }

  static query<T extends NoteToChunksQueryBase>(
    this: new (
      viewer: Viewer,
      src: EdgeQuerySource<Note, NoteChunk>,
    ) => T,
    viewer: Viewer,
    src: EdgeQuerySource<Note, NoteChunk>,
  ): T {
    return new this(viewer, src);
  }

  sourceEnt(id: ID) {
    return Note.load(this.viewer, id);
  }
}

export abstract class NoteToSavedByQueryBase extends AssocEdgeQueryBase<
  Note,
  User,
  NoteToSavedByEdge,
  Viewer
> {
  constructor(viewer: Viewer, src: EdgeQuerySource<Note, User, Viewer>) {
    super(
      viewer,
      src,
      noteToSavedByCountLoaderFactory,
      noteToSavedByDataLoaderFactory,
      User.loaderOptions(),
    );
  }

  static query<T extends NoteToSavedByQueryBase>(
    this: new (
      viewer: Viewer,
      src: EdgeQuerySource<Note, User>,
    ) => T,
    viewer: Viewer,
    src: EdgeQuerySource<Note, User>,
  ): T {
    return new this(viewer, src);
  }

  sourceEnt(id: ID) {
    return Note.load(this.viewer, id);
  }

  queryCreatedWorkspaces(): UserToCreatedWorkspacesQuery {
    return UserToCreatedWorkspacesQuery.query(this.viewer, this);
  }

  queryNotesAuthored(): UserToNotesAuthoredQuery {
    return UserToNotesAuthoredQuery.query(this.viewer, this);
  }

  querySavedNotes(): UserToSavedNotesQuery {
    return UserToSavedNotesQuery.query(this.viewer, this);
  }

  queryWorkspaces(): UserToWorkspacesQuery {
    return UserToWorkspacesQuery.query(this.viewer, this);
  }
}

export abstract class NoteToTagsQueryBase extends AssocEdgeQueryBase<
  Note,
  Tag,
  NoteToTagsEdge,
  Viewer
> {
  constructor(viewer: Viewer, src: EdgeQuerySource<Note, Tag, Viewer>) {
    super(
      viewer,
      src,
      noteToTagsCountLoaderFactory,
      noteToTagsDataLoaderFactory,
      Tag.loaderOptions(),
    );
  }

  static query<T extends NoteToTagsQueryBase>(
    this: new (
      viewer: Viewer,
      src: EdgeQuerySource<Note, Tag>,
    ) => T,
    viewer: Viewer,
    src: EdgeQuerySource<Note, Tag>,
  ): T {
    return new this(viewer, src);
  }

  sourceEnt(id: ID) {
    return Note.load(this.viewer, id);
  }

  queryNotes(): TagToNotesQuery {
    return TagToNotesQuery.query(this.viewer, this);
  }
}

export class AuthorToNotesQueryBase<
  TEnt extends UserBase = UserBase,
> extends CustomEdgeQueryBase<TEnt, Note, Viewer> {
  constructor(
    viewer: Viewer,
    private srcEnt: TEnt,
    sortColumn?: string | OrderBy,
  ) {
    super(viewer, {
      src: srcEnt,
      groupCol: "author_id",
      loadEntOptions: Note.loaderOptions(),
      name: "AuthorToNotesQuery",
      sortColumn: typeof sortColumn === "string" ? sortColumn : undefined,
      orderby: typeof sortColumn === "string" ? undefined : sortColumn,
    });
  }

  static query<
    T extends AuthorToNotesQueryBase,
    TEnt extends UserBase = UserBase,
  >(this: new (viewer: Viewer, src: TEnt) => T, viewer: Viewer, src: TEnt): T {
    return new this(viewer, src);
  }

  async sourceEnt(_id: ID) {
    return this.srcEnt;
  }
}

export class WorkspaceToNotesQueryBase<
  TEnt extends WorkspaceBase = WorkspaceBase,
> extends CustomEdgeQueryBase<TEnt, Note, Viewer> {
  constructor(
    viewer: Viewer,
    private srcEnt: TEnt,
    sortColumn?: string | OrderBy,
  ) {
    super(viewer, {
      src: srcEnt,
      groupCol: "workspace_id",
      loadEntOptions: Note.loaderOptions(),
      name: "WorkspaceToNotesQuery",
      sortColumn: typeof sortColumn === "string" ? sortColumn : undefined,
      orderby: typeof sortColumn === "string" ? undefined : sortColumn,
    });
  }

  static query<
    T extends WorkspaceToNotesQueryBase,
    TEnt extends WorkspaceBase = WorkspaceBase,
  >(this: new (viewer: Viewer, src: TEnt) => T, viewer: Viewer, src: TEnt): T {
    return new this(viewer, src);
  }

  async sourceEnt(_id: ID) {
    return this.srcEnt;
  }
}
