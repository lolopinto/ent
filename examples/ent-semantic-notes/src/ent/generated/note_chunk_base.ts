// Generated by github.com/lolopinto/ent/ent, DO NOT EDIT.

import type {
  Context,
  CustomQuery,
  Data,
  Ent,
  ID,
  LoadEntOptions,
  PrivacyPolicy,
  Viewer,
} from "@snowtop/ent";
import type { Field } from "@snowtop/ent/schema";
import type { NoteChunkDBData } from "./loaders";
import {
  AllowIfViewerPrivacyPolicy,
  loadCustomCount,
  loadCustomData,
  loadCustomEnts,
  loadEnt,
  loadEntX,
  loadEnts,
} from "@snowtop/ent";
import { getFields } from "@snowtop/ent/schema";
import { noteChunkLoader, noteChunkLoaderInfo } from "./loaders";
import { NodeType } from "./types";
import { Note, Workspace } from "../internal";
import schema from "../../schema/note_chunk_schema";

export class NoteChunkBase implements Ent<Viewer> {
  protected readonly data: NoteChunkDBData;
  readonly nodeType = NodeType.NoteChunk;
  readonly id: ID;
  readonly createdAt: Date;
  readonly updatedAt: Date;
  readonly noteId: ID;
  readonly workspaceId: ID;
  readonly ordinal: number;
  readonly content: string;
  readonly tokenCount: number | null;
  readonly embedding: string | null;

  constructor(
    public viewer: Viewer,
    data: Data,
  ) {
    this.id = data.id;
    this.createdAt = data.created_at;
    this.updatedAt = data.updated_at;
    this.noteId = data.note_id;
    this.workspaceId = data.workspace_id;
    this.ordinal = data.ordinal;
    this.content = data.content;
    this.tokenCount = data.token_count;
    this.embedding = data.embedding;
    // @ts-expect-error
    this.data = data;
  }

  __setRawDBData<NoteChunkDBData>(data: NoteChunkDBData) {}

  /** used by some ent internals to get access to raw db data. should not be depended on. may not always be on the ent **/
  ___getRawDBData(): NoteChunkDBData {
    return this.data;
  }

  getPrivacyPolicy(): PrivacyPolicy<this, Viewer> {
    return AllowIfViewerPrivacyPolicy;
  }

  static async load<T extends NoteChunkBase>(
    this: new (
      viewer: Viewer,
      data: Data,
    ) => T,
    viewer: Viewer,
    id: ID,
  ): Promise<T | null> {
    return (await loadEnt(
      viewer,
      id,
      NoteChunkBase.loaderOptions.apply(this),
    )) as T | null;
  }

  static async loadX<T extends NoteChunkBase>(
    this: new (
      viewer: Viewer,
      data: Data,
    ) => T,
    viewer: Viewer,
    id: ID,
  ): Promise<T> {
    return (await loadEntX(
      viewer,
      id,
      NoteChunkBase.loaderOptions.apply(this),
    )) as T;
  }

  static async loadMany<T extends NoteChunkBase>(
    this: new (
      viewer: Viewer,
      data: Data,
    ) => T,
    viewer: Viewer,
    ...ids: ID[]
  ): Promise<Map<ID, T>> {
    return (await loadEnts(
      viewer,
      NoteChunkBase.loaderOptions.apply(this),
      ...ids,
    )) as Map<ID, T>;
  }

  static async loadCustom<T extends NoteChunkBase>(
    this: new (
      viewer: Viewer,
      data: Data,
    ) => T,
    viewer: Viewer,
    query: CustomQuery<NoteChunkDBData>,
  ): Promise<T[]> {
    return (await loadCustomEnts(
      viewer,
      {
        ...NoteChunkBase.loaderOptions.apply(this),
        prime: true,
      },
      query,
    )) as T[];
  }

  static async loadCustomData<T extends NoteChunkBase>(
    this: new (
      viewer: Viewer,
      data: Data,
    ) => T,
    query: CustomQuery<NoteChunkDBData>,
    context?: Context,
  ): Promise<NoteChunkDBData[]> {
    return loadCustomData<NoteChunkDBData, NoteChunkDBData>(
      {
        ...NoteChunkBase.loaderOptions.apply(this),
        prime: true,
      },
      query,
      context,
    );
  }

  static async loadCustomCount<T extends NoteChunkBase>(
    this: new (
      viewer: Viewer,
      data: Data,
    ) => T,
    query: CustomQuery<NoteChunkDBData>,
    context?: Context,
  ): Promise<number> {
    return loadCustomCount(
      {
        ...NoteChunkBase.loaderOptions.apply(this),
      },
      query,
      context,
    );
  }

  static async loadRawData<T extends NoteChunkBase>(
    this: new (
      viewer: Viewer,
      data: Data,
    ) => T,
    id: ID,
    context?: Context,
  ): Promise<NoteChunkDBData | null> {
    return noteChunkLoader.createLoader(context).load(id);
  }

  static async loadRawDataX<T extends NoteChunkBase>(
    this: new (
      viewer: Viewer,
      data: Data,
    ) => T,
    id: ID,
    context?: Context,
  ): Promise<NoteChunkDBData> {
    const row = await noteChunkLoader.createLoader(context).load(id);
    if (!row) {
      throw new Error(`couldn't load row for ${id}`);
    }
    return row;
  }

  static loaderOptions<T extends NoteChunkBase>(
    this: new (
      viewer: Viewer,
      data: Data,
    ) => T,
  ): LoadEntOptions<T, Viewer, NoteChunkDBData> {
    return {
      tableName: noteChunkLoaderInfo.tableName,
      fields: noteChunkLoaderInfo.fields,
      ent: this,
      loaderFactory: noteChunkLoader,
    };
  }

  private static schemaFields: Map<string, Field>;

  private static getSchemaFields(): Map<string, Field> {
    if (NoteChunkBase.schemaFields != null) {
      return NoteChunkBase.schemaFields;
    }
    return (NoteChunkBase.schemaFields = getFields(schema));
  }

  static getField(key: string): Field | undefined {
    return NoteChunkBase.getSchemaFields().get(key);
  }

  async loadNote(): Promise<Note | null> {
    return loadEnt(this.viewer, this.noteId, Note.loaderOptions());
  }

  loadNoteX(): Promise<Note> {
    return loadEntX(this.viewer, this.noteId, Note.loaderOptions());
  }

  async loadWorkspace(): Promise<Workspace | null> {
    return loadEnt(this.viewer, this.workspaceId, Workspace.loaderOptions());
  }

  loadWorkspaceX(): Promise<Workspace> {
    return loadEntX(this.viewer, this.workspaceId, Workspace.loaderOptions());
  }
}
