// Generated by github.com/lolopinto/ent/ent, DO NOT EDIT.

import type { AssocEdgeInputOptions, Ent, ID, Viewer } from "@snowtop/ent";
import type {
  Action,
  Builder,
  Changeset,
  ChangesetOptions,
  OrchestratorOptions,
} from "@snowtop/ent/action";
import {
  Orchestrator,
  WriteOperation,
  saveBuilder,
  saveBuilderX,
} from "@snowtop/ent/action";
import { Note, NoteChunk, Tag, User, Workspace } from "../../..";
import { workspaceLoaderInfo } from "../../loaders";
import { EdgeType, NodeType } from "../../types";
import schema from "../../../../schema/workspace_schema";

export interface WorkspaceInput {
  name?: string;
  slug?: string;
  description?: string | null;
  creatorId?: ID | Builder<User, Viewer>;
  embeddingModel?: string;
  // allow other properties. useful for action-only fields
  [x: string]: any;
}

function randomNum(): string {
  return Math.random().toString(10).substring(2);
}

type MaybeNull<T extends Ent> = T | null;
type TMaybleNullableEnt<T extends Ent> = T | MaybeNull<T>;

export class WorkspaceBuilder<
  TInput extends WorkspaceInput = WorkspaceInput,
  TExistingEnt extends TMaybleNullableEnt<Workspace> = Workspace | null,
> implements Builder<Workspace, Viewer, TExistingEnt>
{
  orchestrator: Orchestrator<Workspace, TInput, Viewer, TExistingEnt>;
  readonly placeholderID: ID;
  readonly ent = Workspace;
  readonly nodeType = NodeType.Workspace;
  private input: TInput;
  private m: Map<string, any> = new Map();

  public constructor(
    public readonly viewer: Viewer,
    public readonly operation: WriteOperation,
    action: Action<
      Workspace,
      Builder<Workspace, Viewer, TExistingEnt>,
      Viewer,
      TInput,
      TExistingEnt
    >,
    public readonly existingEnt: TExistingEnt,
    opts?: Partial<
      OrchestratorOptions<Workspace, TInput, Viewer, TExistingEnt>
    >,
  ) {
    this.placeholderID = `$ent.idPlaceholderID$ ${randomNum()}-Workspace`;
    this.input = action.getInput();
    const updateInput = (d: WorkspaceInput) =>
      this.updateInput.apply(this, [d]);

    this.orchestrator = new Orchestrator({
      viewer,
      operation: this.operation,
      tableName: "workspaces",
      key: "id",
      loaderOptions: Workspace.loaderOptions(),
      builder: this,
      action,
      schema,
      editedFields: () => this.getEditedFields.apply(this),
      updateInput,
      fieldInfo: workspaceLoaderInfo.fieldInfo,
      ...opts,
    });
  }

  getInput(): TInput {
    return this.input;
  }

  updateInput(input: Omit<WorkspaceInput, "creatorId">) {
    if (input.creatorId !== undefined) {
      throw new Error(
        `creatorId cannot be passed to updateInput. use overrideCreatorId instead`,
      );
    }

    // override input
    this.input = {
      ...this.input,
      ...input,
    };
  }

  // override immutable field `creatorId`
  overrideCreatorId(val: ID | Builder<User, Viewer>) {
    this.input.creatorId = val;
  }

  deleteInputKey(key: keyof WorkspaceInput) {
    delete this.input[key];
  }

  // store data in Builder that can be retrieved by another validator, trigger, observer later in the action
  storeData(k: string, v: any) {
    this.m.set(k, v);
  }

  // retrieve data stored in this Builder with key
  getStoredData(k: string) {
    return this.m.get(k);
  }

  // this returns the id of the existing ent or the id of the ent that's being created
  async getEntID() {
    if (this.existingEnt) {
      return this.existingEnt.id;
    }
    const edited = await this.orchestrator.getEditedData();
    if (!edited.id) {
      throw new Error(
        `couldn't get the id field. should have been set by 'defaultValueOnCreate'`,
      );
    }
    return edited.id;
  }
  // this gets the inputs that have been written for a given edgeType and operation
  // WriteOperation.Insert for adding an edge and WriteOperation.Delete for deleting an edge
  getEdgeInputData(edgeType: EdgeType, op: WriteOperation) {
    return this.orchestrator.getInputEdges(edgeType, op);
  }

  clearInputEdges(edgeType: EdgeType, op: WriteOperation, id?: ID) {
    this.orchestrator.clearInputEdges(edgeType, op, id);
  }

  addMember(...nodes: (ID | User | Builder<User, any>)[]): this {
    for (const node of nodes) {
      if (this.isBuilder(node)) {
        this.addMemberID(node);
      } else if (typeof node === "object") {
        this.addMemberID(node.id);
      } else {
        this.addMemberID(node);
      }
    }
    return this;
  }

  addMemberID(
    id: ID | Builder<User, any>,
    options?: AssocEdgeInputOptions,
  ): this {
    this.orchestrator.addOutboundEdge(
      id,
      EdgeType.WorkspaceToMembers,
      NodeType.User,
      options,
    );
    return this;
  }

  removeMember(...nodes: (ID | User)[]): this {
    for (const node of nodes) {
      if (typeof node === "object") {
        this.orchestrator.removeOutboundEdge(
          node.id,
          EdgeType.WorkspaceToMembers,
        );
      } else {
        this.orchestrator.removeOutboundEdge(node, EdgeType.WorkspaceToMembers);
      }
    }
    return this;
  }

  addNoteChunk(...nodes: (ID | NoteChunk | Builder<NoteChunk, any>)[]): this {
    for (const node of nodes) {
      if (this.isBuilder(node)) {
        this.addNoteChunkID(node);
      } else if (typeof node === "object") {
        this.addNoteChunkID(node.id);
      } else {
        this.addNoteChunkID(node);
      }
    }
    return this;
  }

  addNoteChunkID(
    id: ID | Builder<NoteChunk, any>,
    options?: AssocEdgeInputOptions,
  ): this {
    this.orchestrator.addOutboundEdge(
      id,
      EdgeType.WorkspaceToNoteChunks,
      NodeType.NoteChunk,
      options,
    );
    return this;
  }

  removeNoteChunk(...nodes: (ID | NoteChunk)[]): this {
    for (const node of nodes) {
      if (typeof node === "object") {
        this.orchestrator.removeOutboundEdge(
          node.id,
          EdgeType.WorkspaceToNoteChunks,
        );
      } else {
        this.orchestrator.removeOutboundEdge(
          node,
          EdgeType.WorkspaceToNoteChunks,
        );
      }
    }
    return this;
  }

  addNote(...nodes: (ID | Note | Builder<Note, any>)[]): this {
    for (const node of nodes) {
      if (this.isBuilder(node)) {
        this.addNoteID(node);
      } else if (typeof node === "object") {
        this.addNoteID(node.id);
      } else {
        this.addNoteID(node);
      }
    }
    return this;
  }

  addNoteID(
    id: ID | Builder<Note, any>,
    options?: AssocEdgeInputOptions,
  ): this {
    this.orchestrator.addOutboundEdge(
      id,
      EdgeType.WorkspaceToNotes,
      NodeType.Note,
      options,
    );
    return this;
  }

  removeNote(...nodes: (ID | Note)[]): this {
    for (const node of nodes) {
      if (typeof node === "object") {
        this.orchestrator.removeOutboundEdge(
          node.id,
          EdgeType.WorkspaceToNotes,
        );
      } else {
        this.orchestrator.removeOutboundEdge(node, EdgeType.WorkspaceToNotes);
      }
    }
    return this;
  }

  addTag(...nodes: (ID | Tag | Builder<Tag, any>)[]): this {
    for (const node of nodes) {
      if (this.isBuilder(node)) {
        this.addTagID(node);
      } else if (typeof node === "object") {
        this.addTagID(node.id);
      } else {
        this.addTagID(node);
      }
    }
    return this;
  }

  addTagID(id: ID | Builder<Tag, any>, options?: AssocEdgeInputOptions): this {
    this.orchestrator.addOutboundEdge(
      id,
      EdgeType.WorkspaceToTags,
      NodeType.Tag,
      options,
    );
    return this;
  }

  removeTag(...nodes: (ID | Tag)[]): this {
    for (const node of nodes) {
      if (typeof node === "object") {
        this.orchestrator.removeOutboundEdge(node.id, EdgeType.WorkspaceToTags);
      } else {
        this.orchestrator.removeOutboundEdge(node, EdgeType.WorkspaceToTags);
      }
    }
    return this;
  }

  async build(): Promise<Changeset> {
    return this.orchestrator.build();
  }

  async buildWithOptions_BETA(options: ChangesetOptions): Promise<Changeset> {
    return this.orchestrator.buildWithOptions_BETA(options);
  }

  async valid(): Promise<boolean> {
    return this.orchestrator.valid();
  }

  async validX(): Promise<void> {
    return this.orchestrator.validX();
  }

  async save(): Promise<void> {
    await saveBuilder(this);
  }

  async saveX(): Promise<void> {
    await saveBuilderX(this);
  }

  async editedEnt(): Promise<Workspace | null> {
    return this.orchestrator.editedEnt();
  }

  async editedEntX(): Promise<Workspace> {
    return this.orchestrator.editedEntX();
  }

  private async getEditedFields(): Promise<Map<string, any>> {
    const input = this.input;

    const result = new Map<string, any>();

    const addField = function (key: string, value: any) {
      if (value !== undefined) {
        result.set(key, value);
      }
    };
    addField("name", input.name);
    addField("slug", input.slug);
    addField("description", input.description);
    addField("creatorID", input.creatorId);
    if (
      input.creatorId !== undefined ||
      this.operation === WriteOperation.Delete
    ) {
      if (input.creatorId) {
        this.orchestrator.addInboundEdge(
          input.creatorId,
          EdgeType.UserToCreatedWorkspaces,
          NodeType.User,
        );
      }
      if (
        this.existingEnt &&
        this.existingEnt.creatorId &&
        this.existingEnt.creatorId !== input.creatorId
      ) {
        this.orchestrator.removeInboundEdge(
          this.existingEnt.creatorId,
          EdgeType.UserToCreatedWorkspaces,
        );
      }
    }
    addField("embeddingModel", input.embeddingModel);
    return result;
  }

  isBuilder<T extends Ent>(
    node: ID | T | Builder<T, any>,
  ): node is Builder<T, any> {
    return (node as Builder<T, any>).placeholderID !== undefined;
  }

  // get value of name. Retrieves it from the input if specified or takes it from existingEnt
  getNewNameValue(): string {
    if (this.input.name !== undefined) {
      return this.input.name;
    }

    if (!this.existingEnt) {
      throw new Error(
        "no value to return for `name` since not in input and no existingEnt",
      );
    }
    return this.existingEnt.name;
  }

  // get value of slug. Retrieves it from the input if specified or takes it from existingEnt
  getNewSlugValue(): string {
    if (this.input.slug !== undefined) {
      return this.input.slug;
    }

    if (!this.existingEnt) {
      throw new Error(
        "no value to return for `slug` since not in input and no existingEnt",
      );
    }
    return this.existingEnt.slug;
  }

  // get value of description. Retrieves it from the input if specified or takes it from existingEnt
  getNewDescriptionValue(): string | null {
    if (this.input.description !== undefined) {
      return this.input.description;
    }

    return this.existingEnt?.description ?? null;
  }

  // get value of creatorID. Retrieves it from the input if specified or takes it from existingEnt
  getNewCreatorIdValue(): ID | Builder<User, Viewer> {
    if (this.input.creatorId !== undefined) {
      return this.input.creatorId;
    }

    if (!this.existingEnt) {
      throw new Error(
        "no value to return for `creatorId` since not in input and no existingEnt",
      );
    }
    return this.existingEnt.creatorId;
  }

  // get value of embeddingModel. Retrieves it from the input if specified or takes it from existingEnt
  getNewEmbeddingModelValue(): string {
    if (this.input.embeddingModel !== undefined) {
      return this.input.embeddingModel;
    }

    if (!this.existingEnt) {
      throw new Error(
        "no value to return for `embeddingModel` since not in input and no existingEnt",
      );
    }
    return this.existingEnt.embeddingModel;
  }
}
