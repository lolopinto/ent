// Generated by github.com/lolopinto/ent/ent, DO NOT EDIT.

import type { AssocEdgeInputOptions, Ent, ID, Viewer } from "@snowtop/ent";
import type {
  Action,
  Builder,
  Changeset,
  ChangesetOptions,
  OrchestratorOptions,
} from "@snowtop/ent/action";
import {
  Orchestrator,
  WriteOperation,
  saveBuilder,
  saveBuilderX,
} from "@snowtop/ent/action";
import { Note, Tag, Workspace } from "../../..";
import { tagLoaderInfo } from "../../loaders";
import { EdgeType, NodeType } from "../../types";
import schema from "../../../../schema/tag_schema";

export interface TagInput {
  workspaceId?: ID | Builder<Workspace, Viewer>;
  name?: string;
  color?: string | null;
  // allow other properties. useful for action-only fields
  [x: string]: any;
}

function randomNum(): string {
  return Math.random().toString(10).substring(2);
}

type MaybeNull<T extends Ent> = T | null;
type TMaybleNullableEnt<T extends Ent> = T | MaybeNull<T>;

export class TagBuilder<
  TInput extends TagInput = TagInput,
  TExistingEnt extends TMaybleNullableEnt<Tag> = Tag | null,
> implements Builder<Tag, Viewer, TExistingEnt>
{
  orchestrator: Orchestrator<Tag, TInput, Viewer, TExistingEnt>;
  readonly placeholderID: ID;
  readonly ent = Tag;
  readonly nodeType = NodeType.Tag;
  private input: TInput;
  private m: Map<string, any> = new Map();

  public constructor(
    public readonly viewer: Viewer,
    public readonly operation: WriteOperation,
    action: Action<
      Tag,
      Builder<Tag, Viewer, TExistingEnt>,
      Viewer,
      TInput,
      TExistingEnt
    >,
    public readonly existingEnt: TExistingEnt,
    opts?: Partial<OrchestratorOptions<Tag, TInput, Viewer, TExistingEnt>>,
  ) {
    this.placeholderID = `$ent.idPlaceholderID$ ${randomNum()}-Tag`;
    this.input = action.getInput();
    const updateInput = (d: TagInput) => this.updateInput.apply(this, [d]);

    this.orchestrator = new Orchestrator({
      viewer,
      operation: this.operation,
      tableName: "tags",
      key: "id",
      loaderOptions: Tag.loaderOptions(),
      builder: this,
      action,
      schema,
      editedFields: () => this.getEditedFields.apply(this),
      updateInput,
      fieldInfo: tagLoaderInfo.fieldInfo,
      ...opts,
    });
  }

  getInput(): TInput {
    return this.input;
  }

  updateInput(input: Omit<TagInput, "workspaceId">) {
    if (input.workspaceId !== undefined) {
      throw new Error(
        `workspaceId cannot be passed to updateInput. use overrideWorkspaceId instead`,
      );
    }

    // override input
    this.input = {
      ...this.input,
      ...input,
    };
  }

  // override immutable field `workspaceId`
  overrideWorkspaceId(val: ID | Builder<Workspace, Viewer>) {
    this.input.workspaceId = val;
  }

  deleteInputKey(key: keyof TagInput) {
    delete this.input[key];
  }

  // store data in Builder that can be retrieved by another validator, trigger, observer later in the action
  storeData(k: string, v: any) {
    this.m.set(k, v);
  }

  // retrieve data stored in this Builder with key
  getStoredData(k: string) {
    return this.m.get(k);
  }

  // this returns the id of the existing ent or the id of the ent that's being created
  async getEntID() {
    if (this.existingEnt) {
      return this.existingEnt.id;
    }
    const edited = await this.orchestrator.getEditedData();
    if (!edited.id) {
      throw new Error(
        `couldn't get the id field. should have been set by 'defaultValueOnCreate'`,
      );
    }
    return edited.id;
  }
  // this gets the inputs that have been written for a given edgeType and operation
  // WriteOperation.Insert for adding an edge and WriteOperation.Delete for deleting an edge
  getEdgeInputData(edgeType: EdgeType, op: WriteOperation) {
    return this.orchestrator.getInputEdges(edgeType, op);
  }

  clearInputEdges(edgeType: EdgeType, op: WriteOperation, id?: ID) {
    this.orchestrator.clearInputEdges(edgeType, op, id);
  }

  addNote(...nodes: (ID | Note | Builder<Note, any>)[]): this {
    for (const node of nodes) {
      if (this.isBuilder(node)) {
        this.addNoteID(node);
      } else if (typeof node === "object") {
        this.addNoteID(node.id);
      } else {
        this.addNoteID(node);
      }
    }
    return this;
  }

  addNoteID(
    id: ID | Builder<Note, any>,
    options?: AssocEdgeInputOptions,
  ): this {
    this.orchestrator.addOutboundEdge(
      id,
      EdgeType.TagToNotes,
      NodeType.Note,
      options,
    );
    return this;
  }

  removeNote(...nodes: (ID | Note)[]): this {
    for (const node of nodes) {
      if (typeof node === "object") {
        this.orchestrator.removeOutboundEdge(node.id, EdgeType.TagToNotes);
      } else {
        this.orchestrator.removeOutboundEdge(node, EdgeType.TagToNotes);
      }
    }
    return this;
  }

  async build(): Promise<Changeset> {
    return this.orchestrator.build();
  }

  async buildWithOptions_BETA(options: ChangesetOptions): Promise<Changeset> {
    return this.orchestrator.buildWithOptions_BETA(options);
  }

  async valid(): Promise<boolean> {
    return this.orchestrator.valid();
  }

  async validX(): Promise<void> {
    return this.orchestrator.validX();
  }

  async save(): Promise<void> {
    await saveBuilder(this);
  }

  async saveX(): Promise<void> {
    await saveBuilderX(this);
  }

  async editedEnt(): Promise<Tag | null> {
    return this.orchestrator.editedEnt();
  }

  async editedEntX(): Promise<Tag> {
    return this.orchestrator.editedEntX();
  }

  private async getEditedFields(): Promise<Map<string, any>> {
    const input = this.input;

    const result = new Map<string, any>();

    const addField = function (key: string, value: any) {
      if (value !== undefined) {
        result.set(key, value);
      }
    };
    addField("workspaceID", input.workspaceId);
    if (
      input.workspaceId !== undefined ||
      this.operation === WriteOperation.Delete
    ) {
      if (input.workspaceId) {
        this.orchestrator.addInboundEdge(
          input.workspaceId,
          EdgeType.WorkspaceToTags,
          NodeType.Workspace,
        );
      }
      if (
        this.existingEnt &&
        this.existingEnt.workspaceId &&
        this.existingEnt.workspaceId !== input.workspaceId
      ) {
        this.orchestrator.removeInboundEdge(
          this.existingEnt.workspaceId,
          EdgeType.WorkspaceToTags,
        );
      }
    }
    addField("name", input.name);
    addField("color", input.color);
    return result;
  }

  isBuilder<T extends Ent>(
    node: ID | T | Builder<T, any>,
  ): node is Builder<T, any> {
    return (node as Builder<T, any>).placeholderID !== undefined;
  }

  // get value of workspaceID. Retrieves it from the input if specified or takes it from existingEnt
  getNewWorkspaceIdValue(): ID | Builder<Workspace, Viewer> {
    if (this.input.workspaceId !== undefined) {
      return this.input.workspaceId;
    }

    if (!this.existingEnt) {
      throw new Error(
        "no value to return for `workspaceId` since not in input and no existingEnt",
      );
    }
    return this.existingEnt.workspaceId;
  }

  // get value of name. Retrieves it from the input if specified or takes it from existingEnt
  getNewNameValue(): string {
    if (this.input.name !== undefined) {
      return this.input.name;
    }

    if (!this.existingEnt) {
      throw new Error(
        "no value to return for `name` since not in input and no existingEnt",
      );
    }
    return this.existingEnt.name;
  }

  // get value of color. Retrieves it from the input if specified or takes it from existingEnt
  getNewColorValue(): string | null {
    if (this.input.color !== undefined) {
      return this.input.color;
    }

    return this.existingEnt?.color ?? null;
  }
}
