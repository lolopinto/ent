// Generated by github.com/lolopinto/ent/ent, DO NOT EDIT.

import type { Ent, ID, Viewer } from "@snowtop/ent";
import type {
  Action,
  Builder,
  Changeset,
  ChangesetOptions,
  OrchestratorOptions,
} from "@snowtop/ent/action";
import {
  Orchestrator,
  WriteOperation,
  saveBuilder,
  saveBuilderX,
} from "@snowtop/ent/action";
import { Note, NoteChunk, Workspace } from "../../..";
import { noteChunkLoaderInfo } from "../../loaders";
import { EdgeType, NodeType } from "../../types";
import schema from "../../../../schema/note_chunk_schema";

export interface NoteChunkInput {
  noteId?: ID | Builder<Note, Viewer>;
  workspaceId?: ID | Builder<Workspace, Viewer>;
  ordinal?: number;
  content?: string;
  tokenCount?: number | null;
  embedding?: string | null;
  // allow other properties. useful for action-only fields
  [x: string]: any;
}

function randomNum(): string {
  return Math.random().toString(10).substring(2);
}

type MaybeNull<T extends Ent> = T | null;
type TMaybleNullableEnt<T extends Ent> = T | MaybeNull<T>;

export class NoteChunkBuilder<
  TInput extends NoteChunkInput = NoteChunkInput,
  TExistingEnt extends TMaybleNullableEnt<NoteChunk> = NoteChunk | null,
> implements Builder<NoteChunk, Viewer, TExistingEnt>
{
  orchestrator: Orchestrator<NoteChunk, TInput, Viewer, TExistingEnt>;
  readonly placeholderID: ID;
  readonly ent = NoteChunk;
  readonly nodeType = NodeType.NoteChunk;
  private input: TInput;
  private m: Map<string, any> = new Map();

  public constructor(
    public readonly viewer: Viewer,
    public readonly operation: WriteOperation,
    action: Action<
      NoteChunk,
      Builder<NoteChunk, Viewer, TExistingEnt>,
      Viewer,
      TInput,
      TExistingEnt
    >,
    public readonly existingEnt: TExistingEnt,
    opts?: Partial<
      OrchestratorOptions<NoteChunk, TInput, Viewer, TExistingEnt>
    >,
  ) {
    this.placeholderID = `$ent.idPlaceholderID$ ${randomNum()}-NoteChunk`;
    this.input = action.getInput();
    const updateInput = (d: NoteChunkInput) =>
      this.updateInput.apply(this, [d]);

    this.orchestrator = new Orchestrator({
      viewer,
      operation: this.operation,
      tableName: "note_chunks",
      key: "id",
      loaderOptions: NoteChunk.loaderOptions(),
      builder: this,
      action,
      schema,
      editedFields: () => this.getEditedFields.apply(this),
      updateInput,
      fieldInfo: noteChunkLoaderInfo.fieldInfo,
      ...opts,
    });
  }

  getInput(): TInput {
    return this.input;
  }

  updateInput(input: Omit<NoteChunkInput, "noteId" | "workspaceId">) {
    if (input.noteId !== undefined) {
      throw new Error(
        `noteId cannot be passed to updateInput. use overrideNoteId instead`,
      );
    }

    if (input.workspaceId !== undefined) {
      throw new Error(
        `workspaceId cannot be passed to updateInput. use overrideWorkspaceId instead`,
      );
    }

    // override input
    this.input = {
      ...this.input,
      ...input,
    };
  }

  // override immutable field `noteId`
  overrideNoteId(val: ID | Builder<Note, Viewer>) {
    this.input.noteId = val;
  }

  // override immutable field `workspaceId`
  overrideWorkspaceId(val: ID | Builder<Workspace, Viewer>) {
    this.input.workspaceId = val;
  }

  deleteInputKey(key: keyof NoteChunkInput) {
    delete this.input[key];
  }

  // store data in Builder that can be retrieved by another validator, trigger, observer later in the action
  storeData(k: string, v: any) {
    this.m.set(k, v);
  }

  // retrieve data stored in this Builder with key
  getStoredData(k: string) {
    return this.m.get(k);
  }

  // this returns the id of the existing ent or the id of the ent that's being created
  async getEntID() {
    if (this.existingEnt) {
      return this.existingEnt.id;
    }
    const edited = await this.orchestrator.getEditedData();
    if (!edited.id) {
      throw new Error(
        `couldn't get the id field. should have been set by 'defaultValueOnCreate'`,
      );
    }
    return edited.id;
  }

  async build(): Promise<Changeset> {
    return this.orchestrator.build();
  }

  async buildWithOptions_BETA(options: ChangesetOptions): Promise<Changeset> {
    return this.orchestrator.buildWithOptions_BETA(options);
  }

  async valid(): Promise<boolean> {
    return this.orchestrator.valid();
  }

  async validX(): Promise<void> {
    return this.orchestrator.validX();
  }

  async save(): Promise<void> {
    await saveBuilder(this);
  }

  async saveX(): Promise<void> {
    await saveBuilderX(this);
  }

  async editedEnt(): Promise<NoteChunk | null> {
    return this.orchestrator.editedEnt();
  }

  async editedEntX(): Promise<NoteChunk> {
    return this.orchestrator.editedEntX();
  }

  private async getEditedFields(): Promise<Map<string, any>> {
    const input = this.input;

    const result = new Map<string, any>();

    const addField = function (key: string, value: any) {
      if (value !== undefined) {
        result.set(key, value);
      }
    };
    addField("noteID", input.noteId);
    if (
      input.noteId !== undefined ||
      this.operation === WriteOperation.Delete
    ) {
      if (input.noteId) {
        this.orchestrator.addInboundEdge(
          input.noteId,
          EdgeType.NoteToChunks,
          NodeType.Note,
        );
      }
      if (
        this.existingEnt &&
        this.existingEnt.noteId &&
        this.existingEnt.noteId !== input.noteId
      ) {
        this.orchestrator.removeInboundEdge(
          this.existingEnt.noteId,
          EdgeType.NoteToChunks,
        );
      }
    }
    addField("workspaceID", input.workspaceId);
    if (
      input.workspaceId !== undefined ||
      this.operation === WriteOperation.Delete
    ) {
      if (input.workspaceId) {
        this.orchestrator.addInboundEdge(
          input.workspaceId,
          EdgeType.WorkspaceToNoteChunks,
          NodeType.Workspace,
        );
      }
      if (
        this.existingEnt &&
        this.existingEnt.workspaceId &&
        this.existingEnt.workspaceId !== input.workspaceId
      ) {
        this.orchestrator.removeInboundEdge(
          this.existingEnt.workspaceId,
          EdgeType.WorkspaceToNoteChunks,
        );
      }
    }
    addField("ordinal", input.ordinal);
    addField("content", input.content);
    addField("tokenCount", input.tokenCount);
    addField("embedding", input.embedding);
    return result;
  }

  isBuilder<T extends Ent>(
    node: ID | T | Builder<T, any>,
  ): node is Builder<T, any> {
    return (node as Builder<T, any>).placeholderID !== undefined;
  }

  // get value of noteID. Retrieves it from the input if specified or takes it from existingEnt
  getNewNoteIdValue(): ID | Builder<Note, Viewer> {
    if (this.input.noteId !== undefined) {
      return this.input.noteId;
    }

    if (!this.existingEnt) {
      throw new Error(
        "no value to return for `noteId` since not in input and no existingEnt",
      );
    }
    return this.existingEnt.noteId;
  }

  // get value of workspaceID. Retrieves it from the input if specified or takes it from existingEnt
  getNewWorkspaceIdValue(): ID | Builder<Workspace, Viewer> {
    if (this.input.workspaceId !== undefined) {
      return this.input.workspaceId;
    }

    if (!this.existingEnt) {
      throw new Error(
        "no value to return for `workspaceId` since not in input and no existingEnt",
      );
    }
    return this.existingEnt.workspaceId;
  }

  // get value of ordinal. Retrieves it from the input if specified or takes it from existingEnt
  getNewOrdinalValue(): number {
    if (this.input.ordinal !== undefined) {
      return this.input.ordinal;
    }

    if (!this.existingEnt) {
      throw new Error(
        "no value to return for `ordinal` since not in input and no existingEnt",
      );
    }
    return this.existingEnt.ordinal;
  }

  // get value of content. Retrieves it from the input if specified or takes it from existingEnt
  getNewContentValue(): string {
    if (this.input.content !== undefined) {
      return this.input.content;
    }

    if (!this.existingEnt) {
      throw new Error(
        "no value to return for `content` since not in input and no existingEnt",
      );
    }
    return this.existingEnt.content;
  }

  // get value of tokenCount. Retrieves it from the input if specified or takes it from existingEnt
  getNewTokenCountValue(): number | null {
    if (this.input.tokenCount !== undefined) {
      return this.input.tokenCount;
    }

    return this.existingEnt?.tokenCount ?? null;
  }

  // get value of embedding. Retrieves it from the input if specified or takes it from existingEnt
  getNewEmbeddingValue(): string | null {
    if (this.input.embedding !== undefined) {
      return this.input.embedding;
    }

    return this.existingEnt?.embedding ?? null;
  }
}
