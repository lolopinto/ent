import { DeleteAuthCodeActionBase } from "./generated/delete_auth_code_action_base";

// we're only writing this once except with --force and packageName provided
export default class DeleteAuthCodeAction extends DeleteAuthCodeActionBase {}
