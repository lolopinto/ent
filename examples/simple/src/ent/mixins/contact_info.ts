/**
 * Copyright whaa whaa
 * Generated by github.com/lolopinto/ent/ent, DO NOT EDIT.
 */

import {
  ContactInfoBaseMixin,
  IContactInfoBase,
  isContactInfo,
} from "../generated/mixins/contact_info_base";

export { isContactInfo };

type Constructor<T = {}> = new (...args: any[]) => T;

export interface IContactInfo extends IContactInfoBase {
  // add custom fields
}

export function ContactInfoMixin<T extends Constructor>(BaseClass: T) {
  return class ContactInfoMixin
    extends ContactInfoBaseMixin(BaseClass)
    implements IContactInfo {
    // add custom fields implementation
  };
}
