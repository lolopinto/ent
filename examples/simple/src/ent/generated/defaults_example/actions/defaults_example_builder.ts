/**
 * Copyright whaa whaa
 * Generated by github.com/lolopinto/ent/ent, DO NOT EDIT.
 */

import type { Ent, ID } from "@snowtop/ent";
import type {
  Action,
  Builder,
  Changeset,
  ChangesetOptions,
  OrchestratorOptions,
} from "@snowtop/ent/action";
import type { DefaultsPayload } from "../../types";
import type { ExampleViewer as ExampleViewerAlias } from "../../../../viewer/viewer";
import {
  Orchestrator,
  WriteOperation,
  saveBuilder,
  saveBuilderX,
} from "@snowtop/ent/action";
import { DefaultsExample } from "../../..";
import { defaultsExampleLoaderInfo } from "../../loaders";
import { NodeType } from "../../types";
import schema from "../../../../schema/defaults_example_schema";

export interface DefaultsExampleInput {
  creatorId?: ID;
  name?: string;
  perHour?: number;
  hourlyLimit?: number;
  payloads?: DefaultsPayload[];
  // allow other properties. useful for action-only fields
  [x: string]: any;
}

function randomNum(): string {
  return Math.random().toString(10).substring(2);
}

type MaybeNull<T extends Ent> = T | null;
type TMaybleNullableEnt<T extends Ent> = T | MaybeNull<T>;

export class DefaultsExampleBuilder<
  TInput extends DefaultsExampleInput = DefaultsExampleInput,
  TExistingEnt extends
    TMaybleNullableEnt<DefaultsExample> = DefaultsExample | null,
> implements Builder<DefaultsExample, ExampleViewerAlias, TExistingEnt>
{
  orchestrator: Orchestrator<
    DefaultsExample,
    TInput,
    ExampleViewerAlias,
    TExistingEnt
  >;
  readonly placeholderID: ID;
  readonly ent = DefaultsExample;
  readonly nodeType = NodeType.DefaultsExample;
  private input: TInput;
  private m: Map<string, any> = new Map();

  public constructor(
    public readonly viewer: ExampleViewerAlias,
    public readonly operation: WriteOperation,
    action: Action<
      DefaultsExample,
      Builder<DefaultsExample, ExampleViewerAlias, TExistingEnt>,
      ExampleViewerAlias,
      TInput,
      TExistingEnt
    >,
    public readonly existingEnt: TExistingEnt,
    opts?: Partial<
      OrchestratorOptions<
        DefaultsExample,
        TInput,
        ExampleViewerAlias,
        TExistingEnt
      >
    >,
  ) {
    this.placeholderID = `$ent.idPlaceholderID$ ${randomNum()}-DefaultsExample`;
    this.input = action.getInput();
    const updateInput = (d: DefaultsExampleInput) =>
      this.updateInput.apply(this, [d]);

    this.orchestrator = new Orchestrator({
      viewer,
      operation: this.operation,
      tableName: "defaults_examples",
      key: "id",
      loaderOptions: DefaultsExample.loaderOptions(),
      builder: this,
      action,
      schema,
      editedFields: () => this.getEditedFields.apply(this),
      updateInput,
      fieldInfo: defaultsExampleLoaderInfo.fieldInfo,
      ...opts,
    });
  }

  getInput(): TInput {
    return this.input;
  }

  updateInput(input: DefaultsExampleInput) {
    // override input
    this.input = {
      ...this.input,
      ...input,
    };
  }

  deleteInputKey(key: keyof DefaultsExampleInput) {
    delete this.input[key];
  }

  // store data in Builder that can be retrieved by another validator, trigger, observer later in the action
  storeData(k: string, v: any) {
    this.m.set(k, v);
  }

  // retrieve data stored in this Builder with key
  getStoredData(k: string) {
    return this.m.get(k);
  }

  // this returns the id of the existing ent or the id of the ent that's being created
  async getEntID() {
    if (this.existingEnt) {
      return this.existingEnt.id;
    }
    const edited = await this.orchestrator.getEditedData();
    if (!edited.id) {
      throw new Error(
        `couldn't get the id field. should have been set by 'defaultValueOnCreate'`,
      );
    }
    return edited.id;
  }

  async build(): Promise<Changeset> {
    return this.orchestrator.build();
  }

  async buildWithOptions_BETA(options: ChangesetOptions): Promise<Changeset> {
    return this.orchestrator.buildWithOptions_BETA(options);
  }

  async valid(): Promise<boolean> {
    return this.orchestrator.valid();
  }

  async validX(): Promise<void> {
    return this.orchestrator.validX();
  }

  async save(): Promise<void> {
    await saveBuilder(this);
  }

  async saveX(): Promise<void> {
    await saveBuilderX(this);
  }

  async editedEnt(): Promise<DefaultsExample | null> {
    return this.orchestrator.editedEnt();
  }

  async editedEntX(): Promise<DefaultsExample> {
    return this.orchestrator.editedEntX();
  }

  private async getEditedFields(): Promise<Map<string, any>> {
    const input = this.input;

    const result = new Map<string, any>();

    const addField = function (key: string, value: any) {
      if (value !== undefined) {
        result.set(key, value);
      }
    };
    addField("creatorId", input.creatorId);
    addField("name", input.name);
    addField("perHour", input.perHour);
    addField("hourlyLimit", input.hourlyLimit);
    addField("payloads", input.payloads);
    return result;
  }

  isBuilder<T extends Ent>(
    node: ID | T | Builder<T, any>,
  ): node is Builder<T, any> {
    return (node as Builder<T, any>).placeholderID !== undefined;
  }

  // get value of creatorId. Retrieves it from the input if specified or takes it from existingEnt
  getNewCreatorIdValue(): ID {
    if (this.input.creatorId !== undefined) {
      return this.input.creatorId;
    }

    if (!this.existingEnt) {
      throw new Error(
        "no value to return for `creatorId` since not in input and no existingEnt",
      );
    }
    return this.existingEnt.creatorId;
  }

  // get value of name. Retrieves it from the input if specified or takes it from existingEnt
  getNewNameValue(): string {
    if (this.input.name !== undefined) {
      return this.input.name;
    }

    if (!this.existingEnt) {
      throw new Error(
        "no value to return for `name` since not in input and no existingEnt",
      );
    }
    return this.existingEnt.name;
  }

  // get value of perHour. Retrieves it from the input if specified or takes it from existingEnt
  getNewPerHourValue(): number {
    if (this.input.perHour !== undefined) {
      return this.input.perHour;
    }

    if (!this.existingEnt) {
      throw new Error(
        "no value to return for `perHour` since not in input and no existingEnt",
      );
    }
    return this.existingEnt.perHour;
  }

  // get value of hourlyLimit. Retrieves it from the input if specified or takes it from existingEnt
  getNewHourlyLimitValue(): number {
    if (this.input.hourlyLimit !== undefined) {
      return this.input.hourlyLimit;
    }

    if (!this.existingEnt) {
      throw new Error(
        "no value to return for `hourlyLimit` since not in input and no existingEnt",
      );
    }
    return this.existingEnt.hourlyLimit;
  }

  // get value of payloads. Retrieves it from the input if specified or takes it from existingEnt
  getNewPayloadsValue(): DefaultsPayload[] {
    if (this.input.payloads !== undefined) {
      return this.input.payloads;
    }

    if (!this.existingEnt) {
      throw new Error(
        "no value to return for `payloads` since not in input and no existingEnt",
      );
    }
    return this.existingEnt.payloads;
  }
}
