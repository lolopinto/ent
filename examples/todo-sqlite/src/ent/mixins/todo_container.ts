// Generated by github.com/lolopinto/ent/ent, DO NOT EDIT.

import { Viewer } from "@snowtop/ent";
import {
  ITodoContainerBase,
  TodoContainerBaseMixin,
} from "src/ent/generated/mixins/todo_container_base";

export interface ITodoContainer<TViewer extends Viewer = Viewer>
  extends ITodoContainerBase<TViewer> {
  isTodoContainer(): boolean;

  // add custom fields
}

type Constructor<T extends ITodoContainer = ITodoContainer> = new (
  ...args: any[]
) => T;

export function isTodoContainer(ent: unknown): ent is ITodoContainer {
  const o = ent as ITodoContainer;
  return (o.isTodoContainer && o.isTodoContainer()) ?? false;
}

export function TodoContainerMixin<T extends Constructor>(BaseClass: T) {
  return class TodoContainerMixin
    extends TodoContainerBaseMixin(BaseClass)
    implements ITodoContainer
  {
    isTodoContainer() {
      return true;
    }

    // add custom fields implementation
  };
}
