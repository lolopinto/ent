{{/* TODO eventually support not exporting */}}
export enum {{.Name}} {
  {{ range $data := .Values -}} 
    {{ if .Comment -}}
      // {{.Comment}}
    {{ end -}}
    {{.Name}} = {{.Value}},
  {{ end -}}
  {{ range $data := .DeprecatedValues -}}
    {{ if .Comment -}}
      // {{.Comment}}
    {{ end -}}
    // @deprecated {{.Name}} = {{.Value}},
  {{ end -}}
}
